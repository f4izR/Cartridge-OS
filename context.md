# Context for Agents

Read this before working on Cartridge OS. Also read `README.md` (product vision/roadmap), `progress.md` (what's done), and `production-readiness.md` (what's left for shipping).

## What this is

A Windows game launcher: console-dashboard style, fullscreen, controller-first, detects games from every major PC launcher (Steam, Epic, GOG, etc.) plus manual additions. No accounts, fully offline.

## Current repo state

3-project solution (`CartridgeOS.sln`) — `CartridgeOS.Tray` was merged into `CartridgeOS.Launcher` on 2026-08-07 (see the single-instance/tray-core section below) and deleted; don't recreate it, and treat any reference to a separate Tray exe elsewhere as stale unless it's specifically talking about history.
- `src/CartridgeOS.Core` — classlib, targets `net10.0-windows` (bumped from plain `net9.0` once registry access needed it — the whole app is Windows-only, no reason to pretend otherwise). Has `Models/Game.cs`, `Data/GameDatabase.cs` (SQLite, create-table-if-not-exists + AddGame/GetAllGames/UpdateLastPlayed), `ViewModelBase` (CommunityToolkit.Mvvm `ObservableObject`), and `Scanning/SteamVdf.cs` + `Scanning/SteamScanner.cs` (reads Steam's install path from the registry, walks `libraryfolders.vdf` + `appmanifest_*.acf`, games launch via `steam://rungameid/{appid}` — no exe-path resolution needed). No repository interface/abstraction — `GameDatabase` is used directly, add an interface only if a second implementation is ever needed. `SteamScanner` is **not verified against a real Steam install** (none on this dev machine) — only `SteamVdf`'s parsing logic is tested, via synthetic sample text in `Launcher/Services/SteamScannerSelfCheck.cs` (`--self-check-steam`). Also has `Scanning/EpicManifest.cs` + `Scanning/EpicScanner.cs` (reads `%ProgramData%\Epic\EpicGamesLauncher\Data\Manifests\*.item`, JSON via `System.Text.Json`) — gives a direct exe path (unlike Steam's protocol-URI launch) but no artwork source yet, `ArtworkPath` always null. Also **not verified against a real install**; check via `--self-check-epic`. Plus GOG/Ubisoft/EA/Battle.net via `Scanning/InstalledProgramsScanner.cs` (walks the Windows uninstall registry) + `Scanning/PublisherGameScanner.cs` (filters by `Publisher` keyword, one instance per launcher, no subclasses) + `Scanning/ExecutableHeuristics.cs` (guesses the exe as the biggest non-installer `.exe` in the install root — the uninstall registry never gives the exe directly). These four are **meaningfully less trustworthy** than Steam/Epic/Riot: there's no documented manifest to key off, so both the exact `Publisher` string per launcher and the exe-guessing heuristic are unverified assumptions. Plus Riot via `Scanning/RiotManifest.cs` + `RiotScanner.cs` (real documented `RiotClientInstalls.json`, more trustworthy than the four heuristic ones — games route through the Riot Client exe itself). `MainViewModel.ScanForGamesCommand` runs all 7 sources (Steam, Epic, Riot, GOG, Ubisoft, EA, Battle.net) through the shared `ImportScannedGames(IEnumerable<Game>)` dedupe/persist path — add new scanners there too, and to `ScanForGames()`'s `.Concat()` chain, rather than duplicating the merge logic or adding another header button (there's one "Scan for Games" button, not one per launcher — that got consolidated once the count hit 7). **Xbox/Microsoft Store is not implemented** — UWP/MSIX packages don't appear in the uninstall registry at all; would need `Get-AppxPackage`/`PackageManager` APIs, a genuinely different mechanism from every other scanner here.

There's also `Scanning/StandaloneExecutableScanner.cs` — finds games not registered with any launcher at all (the README's "standalone executables" feature) by scanning top-level `Program Files`/`Program Files (x86)` folders for a plausible main exe, blocking known launcher/vendor-hub folder names. This is a pure heuristic with no manifest or registry backing it, so unlike every other scanner it's **not** wired into auto-add — `MainViewModel.FindMoreGamesCommand` routes results through `ScanResultsWindow`/`ScanResultsViewModel`/`CandidateGameViewModel` (a real confirmation dialog, checkboxes default-checked) before anything reaches `ImportScannedGames`. If you add more heuristic (non-manifest) scanners, follow this same "confirm before adding" pattern rather than wiring them into `ScanForGamesCommand`. `CandidateGameViewModel.IsSelected` defaults to `false` — user opts into what's actually a game rather than opting out of false positives (changed from an initial default of `true`). `ScanResultsViewModel` has `SelectAllCommand`/`UnselectAllCommand` for bulk toggling in the dialog. Note: this scanner has no concept of DRM/license status — a cracked exe and a legitimately-purchased DRM-free game look identical to it, by design.

`Scanning/StandaloneExecutableScanner.cs`'s `DefaultScanRoots()` covers more than just the system drive: `Program Files`/`Program Files (x86)` on the system drive, plus `Program Files` and `Games` on every other fixed drive (`DriveInfo.GetDrives()`, `DriveType.Fixed`). Steam/Epic/Riot never needed this fix — they already read real install paths from registry/manifests, which work on any drive by construction.

`Scanning/ArtworkFetcher.cs` — downloads box art once for games with no local `ArtworkPath` and caches it to `%LocalAppData%\CartridgeOS\ArtworkCache\downloaded\{title}_{id}.jpg` (never re-fetched once cached). Steam games (appid parsed back out of the `steam://rungameid/{appid}` exe path) hit Valve's public CDN directly, no API key. Everything else is looked up by title, trying two sources in order: **SteamGridDB first**, **TheGamesDB as fallback** (capped at 1000 requests/month — exactly why it's not primary) — whichever returns null (no match, error, rate-limited) falls through to the next. **Both API keys are hardcoded constants** (`SteamGridDbApiKey`/`TheGamesDbApiKey` at the top of the file) — deliberately not env vars: this is closed-source, the key ships inside the compiled DLL of every install regardless of whether it's a literal or read from an env var, so an env var buys no real protection while adding a setup step that would silently break artwork for every new install. Same reasoning as the Discord Client ID in `DiscordRichPresence.cs` — if you ever open-source this repo, revisit all three. A private nested `RateLimiter` class tracks a per-source cooldown after an HTTP 429 (`Retry-After` header parsed, defaults to 60s if absent) — shared static state across every concurrent fetch in the process, since games are fetched fire-and-forget in a loop after a scan and a burst of many new games would otherwise hammer a source that just rate-limited us. While a source is cooling down it's skipped entirely (falls through immediately), never retried inline/blocking. Logs every step (cache hit, chosen source, non-2xx bodies, 429/backoff, download success/failure) to `%LocalAppData%\CartridgeOS\artwork.log`, plain append, no rotation. **Known weak spot**: both sources' title matching takes the first search result with no disambiguation, can pick the wrong regional/platform/remaster variant. Wired into `MainViewModel` in two places: `ImportScannedGames` (real scan results) and the constructor's initial DB load (so seed/demo games get artwork too) — both fire-and-forget per game via `GameDatabase.UpdateArtworkPath` + `GameTileViewModel.SetArtworkPath` hot-swapping the tile once art lands, never blocking the UI. `SteamScanner.FindArtwork` also got a bug fix here: was only checking the old flat `{appid}_library_600x900.jpg` filename, now checks the newer per-appid nested path (`librarycache/{appid}/library_600x900.jpg`) first — that's why Steam art wasn't showing even with a real local Steam install.

Also `Scanning/XboxScanner.cs` — Xbox app/PC Game Pass/Microsoft Store games. These are UWP/MSIX packages with no registry entry and no simple manifest file (unlike every other scanner), so this shells out to PowerShell's `Get-AppxPackage` (user picked this over native WinRT `PackageManager` interop or a narrower XboxGames-folder-only scan — see conversation history if the tradeoff needs revisiting) rather than reimplementing package-manager logic. Games launch via `shell:appsFolder\{PackageFamilyName}!{AppId}`. Same trust model as `StandaloneExecutableScanner` — no reliable "is this actually a game" signal, so it also routes through `ScanResultsWindow` confirmation, merged together in `MainViewModel.FindMoreGamesAsync` (now genuinely async — spawning PowerShell can take a couple seconds, and this used to run synchronously on the UI thread before Xbox scanning was added, which would now freeze the window). **Not tested against real Xbox/Store games** — by explicit user choice (no such install available), not just an oversight; only self-check-verified.

`MainViewModel` also runs a `DispatcherTimer` (15-minute interval, `ScanTrustedLauncherSources()`) that re-scans the 7 trusted launcher sources in the background — `RescanInBackgroundAsync` wraps the scan in `Task.Run` so it doesn't hitch the UI thread, merges back via the same `ImportScannedGames`. Deliberately excludes `StandaloneExecutableScanner` (would pop a confirmation dialog unprompted). `MainWindow`'s `Closed` handler calls `StopBackgroundRescanning()` — don't forget that if you ever add a second window/entry point that constructs a `MainViewModel`, or the timer outlives its owner.
- `src/CartridgeOS.Launcher` — WPF, borderless fullscreen shell (`WindowStyle=None`, maximized, topmost), `net10.0-windows10.0.19041.0` (bumped from plain `net10.0-windows` on 2026-08-08 to unlock `Windows.Gaming.Input.RawGameController` for PlayStation controller support — see below; `Core`/`Service` are unaffected). Cursor is currently visible (`Cursor="None"` was removed — hiding it broke mouse testing with no way to toggle it back; restore once there's real input-mode detection, show on mouse move / hide on gamepad input). `MainWindow` explicitly calls `Activate()` + `GameGrid.Focus()` in `Loaded` — without it, a debugger-launched window (e.g. from Visual Studio) can end up visible but not receiving keyboard input due to Windows' foreground-lock. Has `ViewModels/MainViewModel.cs` + `GameTileViewModel.cs`, and a virtualized game tile grid in `MainWindow.xaml` (`ListBox` + `VirtualizingWrapPanel` NuGet pkg, ns `WpfToolkit.Controls`) with a scale+glow selection animation. `MainViewModel` currently seeds placeholder titles into the DB if empty — remove that once the V2 scanner exists. Artwork loading is wired: `Services/ArtworkCache.cs` decodes at a fixed pixel width and disk-caches under `%LocalAppData%\CartridgeOS\ArtworkCache\`, keyed by `md5(sourcePath)_{width}.png`; `GameTileViewModel` loads it async and the tile falls back to a placeholder icon if `Artwork` is null (layered `Image` over icon in XAML, no converter). Verified via a synthetic self-check (`--self-check-artwork` launch arg → `Services/ArtworkCacheSelfCheck.cs`, exit 0/1) — **not yet verified with real box art**, since there's no scanner or manual-add UI to attach an `ArtworkPath` to a game yet. If you touch `ArtworkCache`, watch for the WPF UI-thread deadlock we hit: any code that calls `.GetAwaiter().GetResult()` on the UI thread before the Dispatcher message loop is pumping (e.g. in `OnStartup`) will hang unless the awaited method uses `ConfigureAwait(false)` throughout. Controller nav is wired: `Input/XInput.cs` + `Input/GamepadWatcher.cs` (raw P/Invoke of `XInputGetState`, no SharpDX/Vortice dep) poll on a background thread and drive `SelectedGame` via dpad/left-stick, A launches the exe. Column count for up/down nav is derived from `ListBox.ActualWidth` in `MainWindow.xaml.cs` at nav time, not a formal layout abstraction. **Not yet tested with a physical controller** — only verified the not-connected code path doesn't throw. Right-stick mouse emulation is also wired: `GamepadWatcher.RightStickMoved`/`RightTriggerChanged` (right trigger = click, not A — A already launches games) drive `Input/MouseEmulator.cs` (raw `user32.dll` P/Invoke, moves the real system cursor, primary monitor only). `GamepadWatcher.ApplyDeadzone` is `internal` (not `private`) specifically so `--self-check-mouse-emulation` can test the normalization math without hardware; that self-check also does a real (restored-afterward) cursor nudge. Same "not tested with a physical controller" caveat applies — the stick-to-cursor feel (speed constant `MaxPixelsPerTick` in `MouseEmulator.cs`) especially needs real-hands tuning.

**PlayStation controller support + input normalization (2026-08-08)**: XInput never sees DualShock/DualSense — Windows doesn't map them to it. `GamepadWatcher`'s poll loop now falls back to `Input/RawGameControllerSource.cs` (`Windows.Gaming.Input.RawGameController`, WinRT/DirectInput-backed) whenever `XInputGetState` fails, shaping its generic HID reading into the same internal `XInputGamepad` struct so the rest of `GamepadWatcher` (repeat, deadzone, edge-detection) doesn't know or care which source produced it. Button/axis order is a `ponytail`-marked best-effort HID convention (Square/Cross/Circle/Triangle, Share/Options, LX/LY/RX/RY/LT/RT) — not API-guaranteed, recalibrate if a specific pad maps wrong; **not tested against a real PS controller**, no DualShock/DualSense available in this environment. **This required bumping `CartridgeOS.Launcher`'s TFM to `net10.0-windows10.0.19041.0`** (from plain `net10.0-windows` — `CartridgeOS.Core`/`CartridgeOS.Service` are unaffected, still `net10.0-windows`) to unlock the WinRT projection; no new NuGet package needed, the SDK generates it automatically off the TFM's Windows SDK version.

Same change also normalized the input layer end-to-end, per a follow-up request: the UI used to consume physical `GamepadButton` values directly (`ButtonPressed` event, `MainWindow.HandleGamepadButton`). That's now `GamepadAction` (`Input/GamepadAction.cs`: `NavigateUp/Down/Left/Right`, `Confirm`, `Back`, `Secondary`, `Menu`) — `GamepadWatcher.ActionMap` (a `Dictionary<GamepadButton, GamepadAction>`) does the translation once, inside `GamepadWatcher`, so `MainWindow`/`App` never see which physical button did what. `X` and the physical view/select "Back" button are still unmapped — not wired to any launcher behavior, left out rather than inventing unused actions; add an `ActionMap` entry if they get a use. `Input/ControllerKind.cs` (Xbox/PlayStation/Generic, detected via `RawGameController.HardwareVendorId` — Sony is `0x054C`) + `Input/ControllerGlyphs.cs` (per-brand button labels) exist for on-screen prompts; the in-game overlay's toggle hint (`OverlayViewModel.MenuButtonLabel`) is the one real consumer today, showing "Start" or "Options" depending on what's plugged in, kept live via `GamepadWatcher.ControllerChanged` → `App.OnControllerChanged` → the overlay viewmodel.

Controller battery (2026-08-08, part of the status-bar work below): `XInput.GetBatteryPercent` (`XInputGetBatteryInformation`, bucketed to 10/35/65/100% — XInput only reports 4 coarse levels, not a real percentage) and `RawGameControllerSource.GetBatteryPercent` (`RawGameController.TryGetBatteryReport()`, a real percentage from remaining/full milliwatt-hours) feed `GamepadWatcher.ControllerBatteryPercent`/`ControllerBatteryChanged`, checked at most every 10s inside the poll loop (not every 33ms tick — a real syscall each time). `App` forwards that to whichever `MainWindow` currently exists, same pattern as `ControllerChanged`, and pushes the current value on window creation since the window is disposable and recreated. Has a Recent row (`RecentGames`, top 10 by `LastPlayedUtc` desc, collapses via `HasRecentGames`/`BooleanToVisibilityConverter` when empty) above the main grid, sharing `GameTileViewModel` instances with the main grid (same reference, just filtered/sorted — selecting in one reflects in the other). `MainViewModel.RecordPlayed(game)` is the single place that marks a game played (updates `LastPlayedUtc` + persists) and both launch paths (gamepad A, recent-tile double-click) route through the shared `LaunchSelected(vm, game)` helper in `MainWindow.xaml.cs`, which as of 2026-08-07 just forwards to `((App)Application.Current).LaunchGame(vm, game)` — the actual process-start/tracking logic lives in `App` now, see the single-instance/tray-core section below for why. `MainWindow.xaml` has a PS5-menu-style backdrop, and a collapsible settings sidebar as of 2026-08-07 with the first real settings option. Two full-window background `Image`s (`Grid.ColumnSpan="2"` now that the window is split into a content column + sidebar column, `IsHitTestVisible="False"`, dark gradient overlay on top for readability): `BackgroundArt` (bound to `SelectedGame.Artwork`) and `CustomBackgroundArt` (bound to `MainViewModel.CustomWallpaperImage`), toggled via `IsUsingGameArtworkBackground`/`InverseBooleanToVisibilityConverter` (`Converters/InverseBooleanToVisibilityConverter.cs`). Both blurred at `BlurEffect Radius=20` (was 60, cut after user feedback), `Opacity=0.85`, `Stretch="UniformToFill"` (tried `Uniform`/letterboxed briefly, user wanted full-screen fill back). `MainWindow.xaml.cs` fades whichever image actually changed back in (0→0.85, 250ms) via `MainViewModel.PropertyChanged` on `SelectedGame` or `CustomWallpaperImage` — not a true two-image crossfade, see progress.md to-dos.

Settings: `Core/Models/AppSettings.cs` (`WallpaperMode` enum, `CustomWallpaperPath`) + `Core/Data/SettingsStore.cs` persist to a plain JSON file at `%LocalAppData%\CartridgeOS\settings.json` (deliberately not a SQLite table — too small a shape to justify it). `MainViewModel.IsSettingsOpen`/`ToggleSettingsCommand` drive a right-side panel (gear-icon header button, `Border` in the window's sidebar column, plain `Visibility` toggle — no slide-in animation yet). `WallpaperMode` and `ChooseWallpaperCommand` (file picker → `ArtworkCache.LoadAsync` at 1920px, same decode/cache infra artwork tiles use) are the only settings that exist so far. `Converters/EnumToBooleanConverter.cs` binds the two-`RadioButton` wallpaper-mode choice to the single `WallpaperMode` enum property.

**Panel layout (2026-08-07, user asked for Steam/NVIDIA-app-style nesting)**: the panel is a native `TabControl` restyled via `SettingsTabControlStyle` (custom template docks a fixed-width `TabPanel` left as the category rail, content right — no new dependency) and `SettingsTabItemStyle` (category row styling). Each category is a `TabItem` — "Wallpaper" is the only one today. **Add a new category by adding another `TabItem Header="..."` inside the same `TabControl`** (see the comment at that XAML block) and give its content a `SettingsSectionHeadingStyle` heading, same shape as Wallpaper's. Sidebar `Border` is 460px wide to fit the category rail + detail content.
- `src/CartridgeOS.Service` — default Worker Service template, no actual logic yet, targets `net10.0-windows` (bumped alongside Core)

Both Launcher and Service have a project reference to Core. Core/Service target `net10.0-windows`; Launcher targets `net10.0-windows10.0.19041.0` as of 2026-08-08 (see above) — all three are .NET 10 (LTS, supported to ~Nov 2028), matching the README from the start; briefly built against `net9.0`/`net9.0-windows` early on because that was the only SDK installed, bumped once the .NET 10 SDK was installed. `Microsoft.Data.Sqlite` (Core) pins its transitive `SQLitePCLRaw.lib.e_sqlite3` dependency to `2.1.12` directly — the version Sqlite 10.0.10 pulls by default (2.1.11) has a known high-severity CVE (GHSA-2m69-gcr7-jv3q); re-check this pin if `Microsoft.Data.Sqlite` is ever upgraded, a newer release may fix it upstream and make the override unnecessary. Treat any claim of features beyond this as false until verified by reading the actual files.

## Tech decisions (already made — don't re-litigate)

- **UI framework: WPF**, not WinUI 3/Avalonia as the README's table originally suggested. Reason: user only has WPF available in their environment; WPF is sufficiently capable for a modern, fluid, hardware-accelerated fullscreen UI when paired with `WPF-UI` or `ModernWpfUI` for Fluent styling. See conversation history / progress.md for the reasoning.
- **Architecture per README's original diagram**: Windows Service (controller manager, input mapper, game scanner, config manager, IPC server) <-> Tray app <-> Fullscreen Launcher UI, talking over named pipes. **This has since diverged in one specific way (2026-08-07)**: Tray and Launcher were merged into one process (`CartridgeOS.Launcher.exe`) per explicit user request for Steam-style single-instance/tray-core/disposable-UI behavior — see the dedicated section below. The Service half of the diagram (controller manager, scanner, etc. living in a Windows Service) still hasn't happened; scanning/controller input still run inside Launcher. Don't "fix" the Tray/Launcher merge back apart without checking with the user first — it was deliberate, not drift.
- **Solution structure (current)**: `CartridgeOS.Core` (models, SQLite, scanning logic, IPC contracts), `CartridgeOS.Service`, `CartridgeOS.Launcher` (tray core + launcher UI, single process).
- **Data**: SQLite via `Microsoft.Data.Sqlite`, no ORM.
- **MVVM**: `CommunityToolkit.Mvvm` (source-generated, avoid handwritten boilerplate).
- **Controller input**: raw P/Invoke, no XInput NuGet wrapper (`Vortice.XInput`/`SharpDX.XInput` were considered, not used) — `Input/XInput.cs` calls `xinput1_4.dll` directly for Xbox-compatible pads. PlayStation/generic pads don't register with XInput at all, so `Input/RawGameControllerSource.cs` falls back to `Windows.Gaming.Input.RawGameController` (WinRT, DirectInput-backed) — see the dedicated section below for why the Launcher's TFM changed to support this.

## Efficiency constraints (non-negotiable, per user's "must not be slow" requirement)

- Game tile grid MUST be virtualized (`VirtualizingWrapPanel` NuGet, not a plain `WrapPanel`) — libraries can have hundreds of games.
- **Three-screen Home/Recently Played/Library system (2026-08-11), superseded the two-tab interface below.** `MainViewModel.SelectedScreen` (`AppScreen` enum: `Home`/`RecentlyPlayed`/`Library`, default `Home`) replaced `CurrentTab`; `EnumToVisibilityConverter` gates each screen. Nav bar is a centered pill `RadioButton` group (`NavTabStyle`), switchable via mouse, keyboard, or gamepad L1/R1 shoulder buttons (`GamepadAction.PreviousTab`/`NextTab`, `MainWindow.CycleScreen`). **Home**: PS5-style — selected game's own artwork fills the background (this is the only screen where selection drives the backdrop; top+bottom gradient fades keep header/title text legible), bottom-anchored title/details + an infinite horizontal carousel. The carousel is `MainViewModel.HomeCarouselSlots` — a plain computed `List<HomeCarouselSlot>` (game + `IsCenter` bool), bound to a bare `ItemsControl`+`DataTemplate`, **not** a `Selector`/`ScrollViewer`-based `ListBox` — that approach was tried first and kept silently failing to center/resize tiles correctly; the rewrite to explicit computed data was what actually fixed it (see progress.md 2026-08-11 for the failure history). **Recently Played**: hero "Continue Playing" card (highlights via `IsContinuePlayingGameSelected`) + 2x2 grid of the next 4 (`OtherRecentGames`) + a System Overview stats panel, plus an ambient drifting-particle `Canvas` background (`Views/RecentlyPlayedView.xaml.cs`, `TranslateTransform`+`DoubleAnimation`, no dependency — see the particle-bug note below if touching this). **Library**: unchanged virtualized grid.
- **View-layer split into `Views/` (2026-08-11)**: `MainWindow.xaml`/`.xaml.cs` had grown past 1000/300 lines holding markup+handlers for all four screens. Split into `Views/HomeView`, `Views/RecentlyPlayedView`, `Views/LibraryView`, `Views/SettingsPanel` — plain `UserControl`s with **no `DataContext` of their own**, so they inherit `MainViewModel` from the Window and every existing `{Binding ...}` kept working unchanged. `MainWindow` is now a thin shell (~190 lines): background layer, header/nav bar, and `<views:HomeView/>` etc. hosts gated by the same `EnumToVisibility` bindings the inline screen `Grid`s used to have. `LibraryView` exposes `public ListBox GameGrid` since `MainWindow.HandleGamepadAction`'s column-math nav and `OpenGameContextMenu` still need to reach it from the shell. **`MainViewModel` was deliberately NOT split** — almost every property on it (`SelectedGame`, `SelectedScreen`, `Games`/`GamesView`) is genuinely shared across 2+ screens; splitting it would need a new state-sharing mechanism (messenger/mediator) that doesn't exist, for unclear benefit. Shared styles used by 2+ screens live in `Styles/SharedStyles.xaml`; a style used by exactly one screen lives in that screen's own `UserControl.Resources`. **`SharedStyles.xaml` is merged into `App.xaml`'s `Application.Resources`, NOT `MainWindow.xaml`'s `Window.Resources`** — this matters: a `UserControl`'s `InitializeComponent()` resolves `StaticResource` lookups *before* it's attached to its parent's visual tree, so a Window-level merged dictionary isn't visible yet at that point and any UserControl-local style referencing it throws `Cannot find resource named '...'` at startup. App-level resources don't have that timing problem — if you ever add a new cross-screen shared resource, put it in `Styles/SharedStyles.xaml`/`App.xaml`, not `MainWindow.xaml`.
- **Recently Played particle background gotcha**: the drift-loop `StartDrift` originally read `From = transform.Y` when restarting each cycle — but WPF holds an animated property at its final value after the animation completes (no `FillBehavior.Stop`), so that read returned the *previous cycle's `To`*, making `From == To` on every restart after the first (particles silently froze instead of respawning). Fixed by threading the start offset through as an explicit parameter instead of reading the animated property back. If you touch animation-restart-on-`Completed` code anywhere else in this app, this is the failure mode to check for first.
- Artwork must be decoded at target size (`BitmapImage.DecodePixelWidth`) and disk-cached, never loaded full-res per tile.
- Animate only `RenderTransform`/opacity (GPU-composited), never layout properties (`Width`/`Height`/`Margin`).
- All scanning/file IO off the UI thread.

## Roadmap phases (see progress.md for checklist state)

V1: fullscreen launcher, manual games, controller nav. V2: auto-detection across launchers, custom themes, sound. V3: overlay, suspend/resume, Discord RPC, cloud sync, plugins.

## Color scheme (locked — this is the app's theme, not a suggestion)

Built around the logo's dark charcoal background, white typography, and cyan-to-blue gradient. Implemented as WPF resource brushes in `src/CartridgeOS.Launcher/Theme.xaml` — use those `{StaticResource ...}` brushes in XAML, never hardcode these hex values inline.

**Primary brand:** Primary Cyan `#51E7ED` (symbol + "OS", used in the logo), Primary Blue `#49C1ED` (lower gradient blue), Deep Blue Accent `#168DDC` (stronger focus/selection state). Brand Gradient (logo, selected tiles, progress bars): `#51E7ED → #168DDC`.

**Dark interface:** App Background `#0D1016` (main fullscreen bg), Surface `#14171D` (nav bars, large panels), Raised Surface `#1B2028` (cards, menus, overlays), Hover Surface `#242B35` (hovered/focused secondary item), Border `#303946` (subtle panel/tile borders), Overlay Backdrop `rgba(5,8,12,0.82)` (modal/in-game overlay backdrop).

**Text:** Primary Text `#FBFBFB`, Secondary Text `#BCC1C9`, Muted Text `#767D88`, Text on Cyan `#071014` (text on bright buttons).

**Status (kept muted so they don't compete with brand cyan):** Success `#45D69A`, Warning `#F3B84B`, Error `#F05D68`, Information `#49C1ED`.

**Recommended UI usage:**
- Primary button: bg `#51E7ED`, hover `#70F1F3`, pressed `#39BFD2`, text `#071014`.
- Selected game tile: border `#49C1ED`, glow `rgba(73,193,237,0.35)`, background accent `rgba(81,231,237,0.08)`.
- Focused controller-nav element: focus ring `#51E7ED`, outer glow `rgba(81,231,237,0.30)`, ring thickness 3-4px.
- Navigation: active icon `#51E7ED`, inactive icon `#BCC1C9`, disabled icon `#5D6570`, active indicator gradient `#51E7ED → #168DDC`.

Implemented in `src/CartridgeOS.Launcher/Theme.xaml` (merged into `App.xaml`) and applied throughout `MainWindow.xaml` — always use the `{StaticResource ...}` brushes (`BackgroundBrush`, `SurfaceRaisedBrush`, `SurfaceHoverBrush`, `TextPrimaryBrush`, `BrandCyanBrush`, `BrandBlueBrush`, `PrimaryButtonStyle`, etc.), never hardcode these hex values again.

## Single-instance launcher, tray core, disposable UI (2026-08-07)

Built per an explicit, detailed user spec: single-instance app, a persistent lightweight tray/core half and a disposable launcher-UI half, memory released when the UI closes. This is the reason `CartridgeOS.Tray` no longer exists as a separate project — it's now `App.xaml.cs` inside `CartridgeOS.Launcher`.

**Single instance**: `App.OnStartup` takes a `Mutex(@"Global\CartridgeOS.SingleInstance")`. If another process already holds it, this process sends a `"ShowLauncher"` request over a dedicated named pipe (`CartridgeOsPipeServer`/`Client`, pipe name `"CartridgeOS.Launcher.SingleInstance"` — **not** the same pipe as the Service's IPC below, `CartridgeOsPipeServer`'s pipe name is now a constructor parameter defaulting to `CartridgeOsPipeServer.DefaultPipeName` so both can coexist) and exits immediately (`Environment.Exit(0)`) — no window, no tray icon, nothing else happens in that process. The primary instance's `HandleSingleInstanceSignal` dispatches the request onto the UI thread and calls `ShowLauncher()`.

**Core vs disposable UI**: `App` owns everything that must survive the launcher window being closed — `GamepadWatcher`, `MouseEmulator`, `GlobalHotkey`, `DiscordRichPresence`, running-game process tracking, and the whole overlay flow (`ToggleOverlay`/`ReturnToLauncher`/`QuitRunningGame`, moved here from `MainWindow.xaml.cs`). `GlobalHotkey` needs a real native window handle to register `RegisterHotKey` against; since the launcher window can be destroyed at any time, `App` creates a small always-alive, never-shown `_coreWindow` (`Width=0 Height=0 Visibility=Hidden`) purely for this, via `WindowInteropHelper.EnsureHandle()` (not `.Handle`, which returns zero for a window that's never been shown — `GlobalHotkey.cs` was changed to use `EnsureHandle()` to support this). Gamepad action routing also moved to `App`: it subscribes to `GamepadWatcher.ActionPressed` once, handles `GamepadAction.Menu` (toggle overlay) itself, and forwards everything else to `_launcherWindow?.HandleGamepadAction(action)` when a window exists. (As of 2026-08-08 this is `GamepadAction`, not the raw `GamepadButton` this paragraph originally described — see the PlayStation-controller/input-normalization section above.)

`MainWindow.xaml.cs` is now genuinely disposable: no long-lived service references of its own. `App.ShowLauncher()` constructs it (passing the last-selected game's id so it can restore the selection) when `_launcherWindow` is null, or just restores/activates the existing one otherwise — this single method is the answer to "open the launcher" from every caller: initial startup, tray icon click/double-click, the single-instance pipe signal, the overlay's "Return to Cartridge OS" button, and auto-reopen on game exit. `App.OnLauncherClosed()` (wired to the window's `Closed` event, which fires for the X button, Alt+F4, or a programmatic `Close()` — `ShutdownMode="OnExplicitShutdown"` in `App.xaml` means none of those quit the app) captures `_lastSelectedGameId`, drops the window reference, then does one deliberate `GC.Collect()` + `GC.WaitForPendingFinalizers()` pass. That GC call is intentional, not an accident to clean up later: WPF `BitmapImage` isn't `IDisposable`, so there's no per-object dispose loop to write for the freed artwork/decoded images — dropping every reference to the closed window/viewmodel/`GameTileViewModel` graph and forcing a collection *is* the correct way to reclaim that memory promptly instead of waiting for whatever GC happens to run next.

`LaunchGame` (moved off `MainWindow.LaunchSelected`, which now just forwards to `((App)Application.Current).LaunchGame(vm, game)`) starts the process, sets Discord presence, and — **user's explicit choice**: minimizes rather than destroys the launcher window on launch (cheaper, no recreate-flicker; the window can still be fully closed independently via the X button while a game runs, same `OnLauncherClosed` path as any other close) — tracks the process for the overlay, and on exit (`OnGameExited`) clears Discord presence and calls `ShowLauncher()` again automatically (**user's explicit choice**: Steam-like auto-reopen, not stay-closed).

Tray context menu is now just **Open Cartridge OS** / **Exit Cartridge OS** — the old Tray project's "Service Status" item was dropped rather than ported, since mixing the separate/still-unused Service's status into this merged app's menu didn't make sense once Tray and Launcher were the same process. `ExitApplication()` (wired to "Exit Cartridge OS") is the only path that actually calls `Shutdown()` — it cancels the single-instance pipe server, stops the gamepad poll loop, disposes the hotkey/Discord connection, closes the overlay and launcher window, disposes the tray icon, closes the core window, and releases+disposes the mutex, in that order.

**Verified hands-on** (via PowerShell + raw Win32 calls, working around this environment's UI-automation limitation — see below): started the app (one process) → ran the exe again (second process exited immediately, code 0, no duplicate) → sent `WM_CLOSE` via `PostMessage` to the real window handle found through `EnumWindows`/`GetWindowThreadProcessId` (`Get-Process.MainWindowHandle` came back `0` for this borderless/topmost window and couldn't be used directly) → confirmed the window closed but the same PID kept running → ran the exe again → confirmed the *same* process's window reopened. All four core requirements confirmed working, not just compiling. **Not verified by hand**: tray icon visual appearance/context-menu clicks (can't click a real tray icon from here), and the overlay/Discord-presence flow during an actual running game.

## Named-pipe IPC (Service <-> Launcher, and Launcher <-> Launcher for single-instance)

`Core/Ipc/` has a real, working named-pipe transport: `PipeFraming.cs` (length-prefixed UTF-8 framing), `PipeMessages.cs` (`PipeRequest`/`PipeResponse` records), `CartridgeOsPipeServer.cs` (one connection handles exactly one request then closes — not a long-lived duplex session; `pipeName` is a constructor parameter, defaults to `DefaultPipeName`), `CartridgeOsPipeClient.cs` (`SendAsync`, also takes an optional `pipeName`, returns `null` on any failure — nothing listening, timeout, busy — never throws). All IPC classes use `ConfigureAwait(false)` throughout; don't remove it, see the WPF-deadlock note above — the same class of bug bit this code too, before it ever shipped.

`CartridgeOS.Service`'s `Worker.cs` hosts a server on the default pipe name, backed by the actual shared `GameDatabase` (same `%LocalAppData%\CartridgeOS\games.db` the Launcher uses) — `Ping` and `GetGameCount` are real commands, not stubs, reachable via `--ipc-ping` from the Launcher. **Separately**, `App.xaml.cs` hosts a second server on its own pipe name (`"CartridgeOS.Launcher.SingleInstance"`) purely for single-instance signaling — see the section above. These two servers are unrelated; don't conflate them.

**Scope note — the transport is real, the Service-side architecture migration is not.** The README's architecture diagram shows Controller Manager/Input Mapper/Game Scanner/Configuration Manager all living in the Service, with the Launcher talking to them over this pipe. That has **not** happened — scanning (`Core/Scanning/*`) and controller input (`Input/GamepadWatcher.cs`, now owned by `App`) still run directly inside the Launcher process. Migrating that ownership into the Service is a much bigger, separate piece of work (would need to redesign how the Launcher's UI stays in sync with Service-owned state — polling? a persistent duplex connection instead of one-request-per-connection? push notifications on scan completion?) and hasn't been scoped yet. If you pick that up, decide on the sync model first — the current one-shot request/response server as built doesn't support server-initiated push, only client-initiated pull.

Testing note: `Microsoft.Data.Sqlite` pools connections by default — `Dispose()` doesn't release the underlying file handle. Any code (tests, scripts) that creates a temp SQLite file and needs to delete it afterward must call `Microsoft.Data.Sqlite.SqliteConnection.ClearAllPools()` first, or the delete will fail with a sharing violation. `Services/PipeIpcSelfCheck.cs` in Launcher hit this.

## Sound effects

`Services/SoundService.cs` plays `Assets/Sounds/nav.wav` (grid navigation) and `confirm.wav` (game launch) via `System.Media.SoundPlayer`. Both files are **procedurally-generated placeholder tones** (raw PCM sine waves, synthesized via a one-off PowerShell script — not build-integrated), not real sound design — same "derive something usable now, swap later" approach used for the app icon before the logo was available, except here there's no source asset at all, it's fully synthetic. Marked `Content`/`CopyToOutputDirectory` in the Launcher `.csproj` (not `Resource` — `SoundPlayer` needs a real file path). Verified via `--self-check-sound` that the files exist and load without error; **never confirmed anything is actually audible** — no way to check that from this environment.

## Branding assets

`assets/logo/` (repo root, outside any project) has the source files: `logo symbol.png`, `logo with name.png` (both 1536x1024, genuinely alpha-transparent — a viewer showing them as flat gray is just rendering the transparency, not proof the file lacks it — verify with pixel alpha before assuming otherwise), and `cartridge.svg`. `src/CartridgeOS.Launcher/Assets/` has derived files actually used by the app: `app-icon.png` (256x256, cropped/resized from the symbol) and `app.ico`, generated by a one-off PowerShell + `System.Drawing` script (not part of the build — if the source logo changes, redo the crop/resize/ico-export manually) and `logo-with-name.png` — **not** a straight copy anymore: the source `assets/logo/logo with name.png` is a 1536x1024 canvas with a lot of transparent padding around a much smaller logo+text lockup (real content bounds found via alpha-channel scan: roughly x=204-1316, y=338-610), so the Launcher's copy is cropped to that content region (+20px padding) before use — otherwise `Stretch="Uniform"` wastes most of the display height on empty space, which is exactly what made the header logo unreadably small before this was caught. If the source logo changes again, recrop, don't just copy. Tray and Service don't have branding wired yet.

**If the user says they edited a logo file and the app doesn't reflect it**: the `assets/logo/` source files are NOT what the app reads at runtime — WPF `Resource` items embed into the compiled assembly from `src/CartridgeOS.Launcher/Assets/*.png` at build time. Editing only `assets/logo/...` does nothing until that file is re-copied (and, for `logo-with-name.png`, recropped — see above) into `src/CartridgeOS.Launcher/Assets/` and the project rebuilt. This already happened once (`logo-with-name.png`) — check both locations' file sizes/timestamps before assuming a build/cache problem. To verify the fix actually took (not just "it built without error"), extract the embedded manifest resource straight from the compiled DLL and check its byte length matches the new source file — `Assembly.LoadFrom(dllPath).GetManifestResourceStream(...)` via a `System.Resources.ResourceReader` over the `*.g.resources` entry, resource key is the lowercased relative path (e.g. `assets/logo-with-name.png`).

## In-game overlay (Phase 3, started 2026-08-07; moved from MainWindow to App on 2026-08-07)

`Input/GlobalHotkey.cs` — raw `RegisterHotKey`/`WM_HOTKEY` P/Invoke, `Ctrl+Shift+O`, fires even without window focus. Originally registered against `MainWindow`'s handle; **now registered against `App`'s always-alive hidden `_coreWindow`** (see the single-instance/tray-core section above) so the hotkey keeps working even when the launcher window is closed. `OverlayWindow.xaml` + `ViewModels/OverlayViewModel.cs` — borderless, `Topmost`, docked bottom-right via `SystemParameters.WorkArea`, shows game title + Return/Quit buttons. `App` owns the `OverlayWindow` instance and the whole `ToggleOverlay`/`ReturnToLauncher`/`QuitRunningGame` flow now, not `MainWindow`.

`App.LaunchGame` (moved off `MainWindow.LaunchSelected`) tracks the launched `Process`, hooks `Exited`, and minimizes the launcher window on launch (user's explicit choice, not full destroy — see above) so it doesn't sit visually on top of the game (`MainWindow` is `Topmost="True"`, set back in V1 for the kiosk look). `App.OnGameExited` (dispatched from `Process.Exited`, which fires on a threadpool thread) now calls `ShowLauncher()` (auto-reopen, user's choice) rather than just restoring window state, since the window may have been closed entirely while the game was running.

**Known limitation, not a bug to fix casually**: `Process.Start(UseShellExecute: true)` returns `null` for shell-protocol launches — `steam://rungameid/{id}` (every Steam game) and `shell:appsFolder\...` (Xbox/Store games) — so there's no `Process` to attach `Exited` to, and the overlay/hotkey does nothing for those today. Only directly-launched `.exe` paths (manual add, standalone-scanner finds, Epic, the four `PublisherGameScanner` launchers, Riot's client exe) get overlay support. If this needs fixing for Steam specifically, the general approach is polling for a plausible child process by name/window rather than trusting `Process.Start`'s return value — not attempted here, scope it as its own task if wanted.

Controller toggle: `GamepadAction.Menu` (Xbox "Menu"/PS "Options", via `GamepadWatcher.ActionMap[GamepadButton.Start]`) calls `ToggleOverlay()` too — `App.OnGamepadAction` intercepts it itself rather than forwarding it to the launcher window, so this works even with no launcher window open. Works without focus for free, since `GamepadWatcher` polls XInput (and, as of 2026-08-08, `RawGameController` for PlayStation pads) on a background thread regardless of which window has focus — unlike the keyboard hotkey, which needed `RegisterHotKey` specifically to bypass that requirement.

**Not implemented**: overlay likely won't render on top of DirectX-exclusive-fullscreen games despite `Topmost` — real overlays (Discord/Steam) solve this via graphics-API hooking, genuinely out of scope for a `Topmost` WPF window.

## Discord Rich Presence (Phase 3, started 2026-08-07; moved from MainWindow to App on 2026-08-07)

`Services/DiscordRichPresence.cs` talks to Discord's local IPC pipe directly (`\\.\pipe\discord-ipc-{0..9}`, handshake + `SET_ACTIVITY` frames) — no `discord-rpc` dependency. `ClientId` is the real registered Application ID (`1535179486117502976`, not a secret — shared by every Cartridge OS install).

**Debugged 2026-08-08 after the user reported it didn't work on a second PC.** Two real bugs, found by reading the code (no Discord available to test against directly in this environment): `ConnectAsync` read the handshake response frame but never looked at its contents before setting `_ready = true` — Discord replies to a rejected handshake (e.g. bad/unrecognized `ClientId`) with `evt:"ERROR"` rather than closing the pipe, so a rejected handshake was indistinguishable from a successful one and every subsequent activity update silently failed. Separately, `SendActivityAsync` sent `SET_ACTIVITY` but never read Discord's response frame at all — Discord replies to every command (echo on success, `evt:"ERROR"` with a message on failure), and that reply was just left undrained, so there was no way to see why an update failed. Both fixed: `ConnectAsync` now parses the handshake JSON and bails (staying `_ready = false`) if `evt == "ERROR"`; `SendActivityAsync` reads and logs the response to every call. Added `%LocalAppData%\CartridgeOS\discord.log` (`Log()`, same plain-append-no-rotation pattern as `Core/Scanning/ArtworkFetcher.cs`'s `artwork.log`) logging every pipe-connect attempt, the handshake response, and every `SET_ACTIVITY`/clear response — this class had *zero* logging before, which is exactly why "it didn't work" on a second PC was undiagnosable without this fix. Verified the logging mechanism itself works (confirmed `discord.log` gets the correct "no pipe found" line when Discord isn't running, which is the case in this dev environment). **Still not verified against a real running Discord client** — next step if it's still not working after this: check the actual contents of `discord.log` on the machine where it fails. If the handshake response line shows `evt:"ERROR"`, the attached message says why; the most likely single cause is the registered Discord application behind `ClientId` (`developer.discord.com/applications`) no longer existing, being misconfigured, or `ClientId` itself being stale/wrong — that's something to verify directly on the Developer Portal, not something fixable from reading this repo's code alone.

Now owned by `App` (was `MainWindow`) so it survives the launcher window being closed: connects in `App.OnStartup` (fire-and-forget, silently no-ops if Discord isn't running), sets activity right after every game launch via `App.LaunchGame` — including Steam/Xbox shell-launches, since setting activity doesn't need the `Process` handle the way overlay tracking does — and clears it in `App.OnGameExited`, which is only reachable for the subset of launches that do produce a trackable `Process` (see the overlay section above for which those are). So: Steam/Xbox presence will show "Playing X" but won't auto-clear when that game closes, since we have no signal for when it does.

Suspend/Resume (the Phase 3 item before this one) was evaluated and explicitly **not built** — see the 2026-08-07 progress.md log entry for the full reasoning (real console-style suspend isn't possible on Windows for arbitrary processes; the only achievable version is freezing threads via undocumented `NtSuspendProcess`, which risks anti-cheat bans in online games). Don't pick this back up without a narrower, safer scope than "suspend any game."

## Environment limitation: no real UI automation here

This session cannot drive interactive input against a running WPF window and get trustworthy results. `[Microsoft.VisualBasic.Interaction]::AppActivate` / `SendKeys` appear to run without error, but `GetForegroundWindow()` confirms the target window never actually gains focus — keystrokes go nowhere, and a "nothing happened" result from that approach is not evidence of a bug. If you need to verify actual keyboard/mouse/gamepad behavior, say so explicitly and ask the user to test by hand rather than trying to automate it from here again.

**What *does* work from here, found 2026-08-07**: sending real Win32 messages to a window handle doesn't need focus, only a valid `hWnd` — that's a genuinely different mechanism from `SendKeys`/`AppActivate`, which simulate input at the focused-window level. `Get-Process.MainWindowHandle` is unreliable for a borderless/topmost WPF window (came back `0` even for a window that was definitely visible) — instead enumerate real windows via P/Invoke `EnumWindows` + `GetWindowThreadProcessId` filtered to the target PID, then `PostMessage(hwnd, WM_CLOSE=0x0010, 0, 0)` to close it, or similar messages for other verifiable state changes. This is how the single-instance/disposable-launcher-UI work above was actually verified end-to-end (process survives window close, etc.) rather than just left as "builds clean." Doesn't help with keyboard/mouse/gamepad *input* simulation, only with driving a window through documented Win32 messages it already responds to (close, etc.).

## Two launch/scan bugs fixed 2026-08-08, both reported by the user hands-on

**Games launched with the wrong working directory**: `App.LaunchGame`'s `ProcessStartInfo` never set `WorkingDirectory`, so it defaulted to the .NET process's own folder (the Launcher's own `bin\...`). Any manually-added game that resolves assets via a relative path (e.g. `.\data\`) looked for them next to the launcher instead of itself, and failed to start. Fixed by setting `WorkingDirectory = Path.GetDirectoryName(game.ExecutablePath)`.

**A game could get added to the library twice by a single scan**: `MainViewModel.ImportScannedGames` built its "already exists" `HashSet<string>` once from `Games` before the loop and never grew it as new games were imported *within that same call* — so two entries sharing an `ExecutablePath` in one scan batch both passed the "not already present" check (reported as Wallpaper Engine showing up twice; root cause was `SteamScanner` reading the same `appmanifest_*.acf` twice because `libraryfolders.vdf` listed a library path twice with a trailing-slash/casing difference — the fix isn't Steam-specific though, it protects every scanner). Fixed by using `HashSet<string>.Add`'s return value as the check, so the set grows as each game is imported. This is a **forward fix only** — doesn't retroactively deduplicate a game already sitting twice in `games.db`, which is why Remove Game (below) shipped in the same session.

## Remove Game (2026-08-08)

`GameDatabase.DeleteGame(int id)` — plain `DELETE`. `MainViewModel.RemoveGameCommand` confirms via a Yes/No `MessageBox` first (unlike Add Game — deletion is exactly the kind of data-loss-risking action that keeps its confirmation step) then removes the DB row + tile and re-selects. "Remove Game" header button next to "+ Add Game" in `MainWindow.xaml`, operates on whatever's currently `SelectedGame`. **Deliberately not wired**: disabling the button when nothing's selected (would need a new null-to-bool converter for one binding — `RemoveGame()` already no-ops safely on a null selection, so the button just does nothing rather than erroring), and any gamepad/keyboard binding (`X` is still unmapped in `GamepadAction` — deletion via a physical button with no on-screen confirmation felt riskier than worth adding unprompted).

## Header status bar: battery, connectivity, search, clock (2026-08-08)

Built to match a reference design the user supplied, restyled to the app's own `Theme.xaml` palette rather than the reference image's colors (`StatusPillStyle` in `MainWindow.xaml` — rounded `SurfaceRaisedBrush`/`BorderBrush` pill, same brushes every other panel in the app uses). Four pills sit in the header's existing `Grid.Column="2"` `StackPanel`, before the settings/minimize/close buttons.

**Battery**: shows a connected controller's battery if it reports one (see the PlayStation-controller section above for how `GamepadWatcher.ControllerBatteryPercent` gets populated), otherwise falls back to this machine's own battery via `Services/DeviceBattery.cs` (raw `GetSystemPowerStatus` P/Invoke — same "no WinRT/WinForms needed" convention as the rest of `Input/`), and hides the pill entirely on a desktop with neither. `MainViewModel.RefreshBatteryDisplay()` combines the two into `BatteryGlyph`/`BatteryLabel`/`HasBatteryInfo`, recomputed on the 1s clock tick and whenever `ControllerBatteryPercent` changes.

**Connectivity (rewritten 2026-08-11 after two rounds of "still shows online" reports)**: `MainViewModel.IsOnline` used to be driven directly by `NetworkChange.NetworkAvailabilityChanged`/`NetworkInterface.GetIsNetworkAvailable()`. Both are **adapter-state** signals, not real internet reachability — `GetIsNetworkAvailable()` stays `true` as long as some interface is "up," even if that interface is still associated to a router whose own WAN/ISP link is down, which is exactly how most people actually "disconnect the internet." Fixed with a real reachability probe: `MainViewModel.ProbeConnectivityAsync` (a dedicated 5s `DispatcherTimer`, `_connectivityTimer`) does a short-timeout (`HttpClient`, 3s) `GET` against `http://www.msftconnecttest.com/connecttest.txt` — Windows' own NCSI connectivity-check endpoint, the same one the system tray Wi-Fi flyout uses — and caches the result in `_isInternetReachable`. `IsOnline` (via `RefreshConnectivityDisplay()`) is now `GetIsNetworkAvailable() && _isInternetReachable`, recomputed every 1s clock tick (cheap — local check + cached probe result) and immediately re-probed whenever `NetworkChange.NetworkAvailabilityChanged` fires (fast-path for real adapter disconnects, but the event's own `IsAvailable` flag is no longer trusted directly). `_connectivityTimer` is started/stopped alongside `_statusTimer` in the constructor/`StopStatusUpdates()`. Originally showed swapping emoji; **user asked for the real Wi-Fi icon instead** — now a fixed Segoe MDL2 Assets glyph (`&#xE701;`, same icon font as every other header button) that turns `ErrorBrush`-red when offline via a `DataTrigger` in `MainWindow.xaml`, rather than swapping glyphs.

**Search**: `MainViewModel.GamesView` (`CollectionViewSource.GetDefaultView(Games)`, `Filter` keyed on `SearchText`) is what the game grid actually binds to now, not `Games` directly. The one non-trivial interaction this caused: `MainWindow.HandleGamepadAction`'s dpad/stick nav used to index into `vm.Games` directly — now it walks a `vm.GamesView.Cast<GameTileViewModel>().ToList()` snapshot instead, so gamepad/keyboard nav stays in sync with whatever the search filter is currently showing on screen (matches Steam-style filtered nav, not a formal abstraction). The search icon toggles `IsSearchOpen`, which shows/hides an inline `TextBox` inside the pill and clears `SearchText` on close so the grid doesn't stay stuck filtered. **2026-08-11: the whole search pill is now Library-only** (`Visibility` in `MainWindow.xaml` gated on `SelectedScreen` via `EnumToVisibility`) — user pointed out search only actually filters the Library grid, so showing it on Home/Recently Played implied it did something there too. `SelectedScreen`'s setter now also force-closes search (`IsSearchOpen = false`, which clears `SearchText`) whenever the screen changes away from `Library` — otherwise a search left active while switching screens would keep silently filtering `GamesView` (and therefore Home's carousel / Recently Played's nav list, both of which still walk it) with no visible search box left to explain why.

**Clock**: `MainViewModel._statusTimer` (1s `DispatcherTimer`) formats `CurrentTimeText`/`CurrentDateText` and piggybacks the device-battery refresh on the same tick. Stopped via `MainViewModel.StopStatusUpdates()`, called from `MainWindow`'s `Closed` handler alongside the pre-existing `StopBackgroundRescanning()` — same disposable-window timer-lifecycle discipline as everything else in this app (see the single-instance/tray-core section: the window gets destroyed and recreated, any timer/subscription it (or its viewmodel) owns must be torn down on close or it leaks/keeps firing against a dead window).

Caught and fixed a real latent bug while wiring the battery forwarding: `App.OnControllerChanged` was setting a bound viewmodel property from `GamepadWatcher`'s background poll thread with no `Dispatcher.BeginInvoke` — same class of cross-thread WPF-binding bug `OnGamepadAction` already guarded against, just missed on this one. Fixed by wrapping both `OnControllerChanged` and the new `OnControllerBatteryChanged` in `Dispatcher.BeginInvoke`.

**Not visually verified against the reference image** — no screenshot capability in this environment; check pill spacing/alignment and the 140px search box by hand.

## Storage drive picker (2026-08-11)

Recently Played's System Overview panel used to hardcode `Path.GetPathRoot(Environment.SystemDirectory)` for the storage stat — wrong for anyone whose games live on a non-system drive. `AppSettings.StorageDriveLetter` (persisted, `null` = "use the system drive"), `MainViewModel.StorageDrives` (`ObservableCollection<string>`, populated at startup from every ready `DriveInfo` with `DriveType.Fixed`) + `SelectedStorageDrive` (setter persists to `_settings` and re-runs `RefreshStorageStats`), a new "Storage" section in `SettingsPanel.xaml`'s Library tab (plain `ComboBox`, no Browse — unlike the scan-directory picker, drives aren't browsable paths, `DriveInfo.GetDrives()` already enumerates the full valid set). `RefreshStorageStats` now reads `SelectedStorageDrive ?? systemDriveRoot`.

**Startup default is the system drive, not null** — first attempt left `SelectedStorageDrive` as `null` when nothing was saved yet (correct for `RefreshStorageStats`'s fallback), but that renders as an empty `ComboBox.SelectedItem` even though the system-drive stats were displaying correctly underneath — user reported "nothing was selected." Fixed by explicitly setting `SelectedStorageDrive` to the system drive's root at startup instead of `null`, so the combo box reflects what's actually shown from the first frame.

`StorageDriveLabel` (drive letter minus the trailing backslash, e.g. "D:" from "D:\") feeds the "Storage Used" sub-label in `RecentlyPlayedView.xaml` (`StringFormat='Storage Used ({0})'`) — added after the user pointed out the panel gave no indication of *which* drive the percentage was for.

## Library was missing mouse launch entirely (2026-08-11)

User reported "I played a game and Recently Played didn't update" — traced to `LibraryView`'s `GameTileStyle` never having a click-to-launch handler at all, unlike Home's carousel (`HomeCarouselTile_Click`) and Recently Played's tiles (`RecentGame_DoubleClick`/`ContinuePlayingCard_Click`). Only a `PreviewMouseRightButtonDown` handler existed (for the context menu). A mouse user double-clicking a Library tile just selected it via the `ListBox`'s native behavior and nothing else happened — no launch, so `App.LaunchGame`/`MainViewModel.RecordPlayed` never ran. Keyboard/gamepad launching on Library was unaffected (`MainWindow.HandleGamepadAction`'s `Confirm` branch always worked). Fixed with `MouseDoubleClick="GameGridListBox_MouseDoubleClick"` on the `ListBox` in `LibraryView.xaml`, calling `MainWindow.LaunchSelected(vm, vm.SelectedGame)` — same shape as `RecentGame_DoubleClick`. **If a screen's tile grid/carousel is ever added in the future, remember it needs its own explicit launch wiring — there's no shared base behavior for this across `Views/`, each screen's code-behind owns it independently.**

## Recently Played's 2x2 grid selection fighting the hero card (2026-08-11)

Same root-cause family as the Home-carousel rewrite (see "PS5-style carousel" section far below) — a `Selector`'s own `SelectedItem`/`IsSelected` state doesn't reliably follow an externally-set source property once two-way binding is in the mix. Reported with a screenshot: user selected the hero card (`ContinuePlayingCard_Click` sets `vm.SelectedGame = vm.ContinuePlayingGame`), but the 2x2 grid's `RecentRowStyle` kept its `IsSelected`-driven cyan border on whatever row it had last selected — because the hero's game isn't a member of `OtherRecentGames` (`RecentGames.Skip(1).Take(4)`), so the `ListBox`'s own selection state had nothing to reconcile against and just didn't change, even though its two-way `SelectedItem="{Binding SelectedGame}"` binding *had* pushed the new value into the source property correctly.

Fixed by removing the `ListBox`'s reliance on `Selector` state entirely, same pattern as the carousel: dropped `SelectedItem="{Binding SelectedGame}"` from the `ListBox` in `RecentlyPlayedView.xaml`; each row now sets `SelectedGame` explicitly via a `MouseLeftButtonUp` `EventSetter` (`RecentTile_Click`, identical shape to `HomeCarouselTile_Click`); the border highlight is now a `DataTrigger` whose `Binding` is a `MultiBinding` (new `Converters/EqualsMultiConverter.cs`, `IMultiValueConverter` returning `Equals(values[0], values[1])`) comparing `{Binding}` (the row's own `GameTileViewModel`) against `{Binding DataContext.SelectedGame, RelativeSource={RelativeSource AncestorType=ListBox}}` (walks up to the `ListBox` to read the shared `MainViewModel`'s current `SelectedGame`). This is the same "stop trusting `Selector` state, compare against the VM's source-of-truth property directly via computed data" fix already applied to the Home carousel — **if another selection-highlight bug shows up anywhere else that uses `ListBox.SelectedItem`/`IsSelected` two-way binding, this is the first thing to suspect and the established fix pattern to reach for.**

Also gave the hero card its own selected-state border (previously only `Background` switched to `HeroGradientBrush` — no `BorderBrush`/`BorderThickness` at all) via the same `Style.Triggers` `DataTrigger` on `IsContinuePlayingGameSelected`, and bumped selection-border `BorderThickness` from 1 (2x2 grid rows)/2 (Library tiles) up to 3 everywhere, matching what `HomeCarouselItemTemplate` already used — per explicit user request to make selection thickness consistent app-wide.

## Per-game context menu: Change Wallpaper / Delete Game (2026-08-08)

Right-click a tile, or press gamepad Menu/Start/Options (keyboard: Apps key) while no game is running, to open a small context menu with "Change Wallpaper" (replace that specific game's artwork with a local image file) and "Delete Game" (reuses the existing Remove Game flow from above).

`MainViewModel.ChangeArtworkCommand` → `ChangeArtwork()` — `OpenFileDialog` then `GameDatabase.UpdateArtworkPath` + `GameTileViewModel.SetArtworkPath`, the exact same DB-update-then-hot-swap-the-tile pattern `ArtworkFetcher`'s background fetch already uses (`FetchArtworkInBackgroundAsync`). **Naming note**: called "Change Wallpaper" in the UI (matching the user's own wording for the request) but implemented as per-game `ArtworkPath`, not `AppSettings.CustomWallpaperPath` — don't confuse this with the pre-existing Settings-sidebar "Choose Wallpaper" (`ChooseWallpaperCommand`), which sets the app-wide custom background image and is a completely separate setting. Since `MainWindow.xaml`'s `BackgroundArt` is bound to `SelectedGame.Artwork`, changing a game's artwork here does also change what shows as the blurred background when that game is selected — which is presumably why the user's mental model called it "wallpaper" in the first place.

`MainWindow.xaml`: one shared `ContextMenu` resource (`GameTileContextMenu`) attached to both `GameTileStyle` and `RecentTileStyle` via a `ContextMenu` `Setter`. Its `MenuItem`s use code-behind `Click` handlers (`ChangeWallpaper_Click`/`DeleteGame_Click`), not XAML bindings — **deliberately**, since `ContextMenu` is a separate popup outside the normal visual tree and reliably binding into the owning window's `DataContext` from inside one is a known WPF headache; reading `MainWindow`'s own `DataContext` field from code-behind sidesteps the whole class of problem rather than fighting `RelativeSource`/`PlacementTarget` bindings. WPF's `ListBox` doesn't move selection on a right-click by itself, so a `PreviewMouseRightButtonDown` `EventSetter` (`GameTile_PreviewMouseRightButtonDown`) selects the clicked tile first — this keeps both `ChangeArtworkCommand` and `RemoveGameCommand` parameterless (operating on `SelectedGame`, same as the header buttons) rather than needing a `RelayCommand<GameTileViewModel>` variant.

**Gamepad `Menu` now means two different things depending on context**, per this feature: `App.OnGamepadAction` used to unconditionally call `ToggleOverlay()` on `GamepadAction.Menu` (a silent no-op whenever no game was running, since `ToggleOverlay` itself early-returns on `_runningGameProcess is null` — effectively dead input the whole time up to this point). Now it only does that when a game IS running; otherwise it forwards `Menu` to the launcher window, which opens the selected tile's context menu (`MainWindow.OpenGameContextMenu`, finds the tile's realized container via `GameGrid.ItemContainerGenerator.ContainerFromItem` — relies on `ScrollIntoView` having already been called during nav, which it always is, so the selected item's container is realized under `VirtualizingWrapPanel`). If you ever add a third meaning for `Menu`, this is the branch point to extend.

**Not hands-on verified** — no way to right-click a real window or press a physical gamepad button from this environment. Check: context menu actually opens and both items work, and that the in-game overlay toggle (unchanged behavior when a game IS running) wasn't broken by this.

## Artwork crop/reposition (2026-08-08)

Follow-up to "Change Wallpaper" above: the tile's `Image` uses `Stretch="UniformToFill"`, which auto-crops to fill — fine for artwork that's already roughly the tile's shape, but silently centers the crop for anything else, which can cut off the interesting part of an unrelated-aspect-ratio image with no user control. `ArtworkCropWindow.xaml`/`.xaml.cs` fixes that: a 300x450 (2:3) viewport the user pans (drag) and zooms (slider, clamped to never zoom out past fully covering the viewport — a deliberate "cover" constraint, not a bug, so there's never an empty gap at an edge) before confirming. On confirm it computes the source-pixel crop rect from the current `ScaleTransform`/`TranslateTransform` values, crops via `CroppedBitmap`, and writes a new PNG to `%LocalAppData%\CartridgeOS\ArtworkCache\custom\{guid}.png` — a plain code-behind dialog (`DialogResult`/`ResultPath`, same shape as the existing `ScanResultsWindow`), no separate ViewModel, since this is inherently view-state (drag position, zoom level) with no other consumer.

`MainViewModel.ChangeArtwork()` only opens this dialog when the picked file's aspect ratio is more than 5% off the 2:3 target (`MatchesTileAspect`/`TileAspect`/`AspectTolerance` constants) — checked via `BitmapFrame.Create(uri, BitmapCreateOptions.DelayCreation, BitmapCacheOption.None)`, which reads just the image header for `PixelWidth`/`PixelHeight` without a full decode, cheap enough to run synchronously on the UI thread for a single file. An image that's already close to the tile's shape skips the crop step entirely and uses the picked file as-is — no forced extra click for the common case of art that's already sized right.

**Not hands-on verified** — no way to drag a mouse, see rendering, or confirm the crop math is pixel-accurate from this environment. If the crop looks off after real use: the likely culprit is the `2.0/3.0`/300x450 viewport constant not actually matching the live tile artwork slot's real rendered aspect ratio (never precisely measured at runtime, just approximated from the box-art convention other parts of this app already assume) — `Stretch="UniformToFill"` on the tile will still auto-crop-to-fill regardless of small mismatches, so it's a "slightly wrong center point" risk, not a broken-image risk.

## Artwork revert (2026-08-08)

Follow-up to the two artwork sections above, prompted by the obvious next question: what if the user changes a game's artwork and doesn't like it? `GameTileViewModel` now remembers one prior `ArtworkPath` (`_previousArtworkPath`/`_hasPreviousArtwork`, set inside `SetArtworkPath` right before overwriting) and exposes `TryRevertArtwork(out restoredPath)` to restore-and-forget it — deliberately a **single undo level, not a history stack**: reverting twice in a row does nothing on the second call. "Revert to Previous Artwork" sits in the same context menu as "Change Wallpaper"/"Delete Game" (`MainWindow.xaml`'s `GameTileContextMenu`), same code-behind-`Click`-handler pattern as those two.

`GameDatabase.UpdateArtworkPath`'s parameter became `string?` (was `string`) to support reverting a game that had no artwork at all before its most recent change — writes `DBNull` on null. `TryRevertArtwork` also explicitly nulls the viewmodel's `Artwork` property in that case, since `GameTileViewModel.LoadArtworkAsync` intentionally no-ops on a null/empty `ArtworkPath` (early-return, doesn't clear the previously-loaded bitmap) — without that explicit null-out, reverting-to-nothing would leave the old image showing.

**Important scope limit, not an oversight**: the "previous artwork" memory lives only on the in-memory `GameTileViewModel` for the current window session. Since the launcher window (and its whole viewmodel graph) is disposable and gets destroyed/recreated on close/reopen (see the single-instance/tray-core section), this memory does not survive that — closing and reopening the launcher, or restarting the app, forgets what the "previous" artwork was. If cross-session undo is ever wanted, it would need a real column/history table in `GameDatabase`, not just an in-memory field — hasn't been asked for, don't build it speculatively.

## Context menu theming (2026-08-08)

The native WPF `ContextMenu`/`MenuItem` chrome is Win32-default (gray, an icon/checkmark gutter column, square corners, system blue highlight) — looked jarringly off-theme against the rest of the app once the per-game context menu (see above) actually shipped. Fixed with two **implicit** styles in `MainWindow.xaml` — `<Style TargetType="ContextMenu">` and `<Style TargetType="MenuItem">`, deliberately no `x:Key` — so they apply automatically to any menu anywhere in the app (there's only the one today, but this makes a second one theme-correct for free rather than needing its own explicit `Style=` reference). `ContextMenu`'s template is a rounded `SurfaceRaisedBrush` `Border` with a `DropShadowEffect`; `MenuItem`'s template drops the native icon/check gutter entirely (every item here is a plain text action, nothing ever gets a checkmark or icon) in favor of a flat `Border` that highlights `SurfaceHoverBrush` on `IsHighlighted`. This is a general app-wide default, not scoped to `GameTileContextMenu` specifically — keep that in mind if a future menu ever needs icons/checkable items, since the current `MenuItem` template has nowhere to put them and would need a second, non-implicit style for that case rather than fighting this one.

**Not visually verified** — no screenshot capability here; open the context menu by hand and confirm it reads as part of the app now, not as a stock Windows menu.

## Context menu item enabled/disabled state (2026-08-08)

"Revert to Previous Artwork" used to always be clickable, silently no-opping via `TryRevertArtwork`'s `false` return when there was nothing to revert — user asked for it to actually be disabled instead. `GameTileViewModel.HasPreviousArtwork` went from a private field to a public property for this. `MainWindow.xaml`'s `GameTileContextMenu` gained `Opened="GameTileContextMenu_Opened"`: reads `((ContextMenu)sender).PlacementTarget`'s `DataContext` (the `GameTileViewModel` the menu is opening for — set natively by right-click, or explicitly by `MainWindow.OpenGameContextMenu` for the gamepad path, so this one handler covers both), finds the specific `MenuItem`, and sets its `IsEnabled`.

**First attempt used `menu.FindName("RevertArtworkMenuItem")` and didn't work** — the user reported the item was "still the same" (always clickable) after it shipped. Root cause: a `MenuItem` declared with `x:Name` inside a `Window.Resources` object graph (as opposed to inside a `ControlTemplate`, which auto-creates a `NameScope`) is **not** registered anywhere `FindName` can resolve at runtime — `FindName` silently returned `null`, so the `if` guarding `IsEnabled` never ran, and the item just kept its XAML-default `IsEnabled="True"` forever. Nothing threw, nothing looked wrong in the diff — this is exactly the kind of bug that survives a build-clean/app-runs smoke test and only surfaces from an actual click. **Fixed with a `Tag`-based lookup instead**: `Tag="RevertArtwork"` in XAML, `menu.Items.OfType<MenuItem>().FirstOrDefault(mi => Equals(mi.Tag, "RevertArtwork"))` in code-behind — `menu.Items` enumeration works regardless of naming/`NameScope`, since these are literal `MenuItem` objects already present as `Items`, not generated from a `DataTemplate`. **If a future context/popup menu in this app needs to reach a specific item from code-behind, use `Tag` + `Items` enumeration, not `x:Name`/`FindName`, unless it's genuinely inside a `ControlTemplate`.**

The custom `MenuItem` template from the theming pass above (see "Context menu theming") had no disabled-state visuals at all — WPF doesn't give you that for free once you replace the default `ControlTemplate`. Added an `IsEnabled="False"` trigger: muted-gray text (`TextMutedBrush`), the hover-highlight `Setter` overridden back to transparent (otherwise a disabled item could still light up gray on mouse-over, which reads as "actually clickable"), and an arrow cursor instead of the hand cursor every other menu item uses.

## Modal-dialog gamepad routing + crop dialog controller support (2026-08-08)

The artwork crop dialog (see above) was mouse-only — user asked for controller support. This surfaced a real gap in the input-normalization work: `GamepadWatcher.ActionPressed`/`RightStickMoved` were wired straight from `App` to whichever the launcher window / mouse-cursor-emulation, with no concept of "something modal is currently on top and should own input instead." Fixed generally, not just for this one dialog:

**New `Input/IGamepadInputTarget.cs`** (`HandleAction(GamepadAction)`, `HandleRightStick(float, float)`) + **`App.SetModalGamepadTarget(IGamepadInputTarget?)`**. While a target is set, `App.OnGamepadAction`/`OnRightStickMoved` route there exclusively instead of `_launcherWindow`/the mouse-emulation cursor, and `OnRightTriggerChanged` suppresses the emulated click entirely (avoids a stray click landing on whatever's underneath). `App`'s `MouseEmulator` and the `RightStickMoved`/`RightTriggerChanged` subscriptions had to move from `OnStartup` locals/inline lambdas to a field (`_mouse`) and named handler methods to support the branch — any future modal window that wants gamepad input follows the same two-line pattern `ArtworkCropWindow` uses: `Loaded += (_, _) => ((App)Application.Current).SetModalGamepadTarget(this);` / `Closed += (_, _) => ((App)Application.Current).SetModalGamepadTarget(null);`, and implement the two interface methods.

**`ArtworkCropWindow`'s actual control scheme** deliberately reuses existing `GamepadAction`/stick plumbing rather than adding new physical-button support: right stick pans directly (same physical stick as mouse-cursor emulation, just a different meaning while this dialog owns input — no on-screen cursor involved here); D-pad up/down zooms via `GamepadAction.NavigateUp`/`NavigateDown`, which `GamepadWatcher` already fires with repeat-while-held, so holding a direction zooms continuously with zero new repeat-timer code; `Confirm`/`Back` map to the same "Use This Crop"/"Cancel" logic the mouse buttons call. There was no way to add analog-trigger or shoulder-button (LB/RB) zoom without extending `GamepadButton`/`XInputGamepad` bit-reading, which wasn't worth it when D-pad zoom was already free.

**Not hands-on tested with a physical controller** — same standing caveat as the rest of the gamepad code. `StickPanPixelsPerTick`/`DPadZoomStepFactor` (`ArtworkCropWindow.xaml.cs`) are unverified-by-feel constants, most likely to need adjustment once someone actually uses this with a controller.

## Minimize-on-launch bug for shell launches (2026-08-08)

Pre-existing bug, not introduced by anything in this session — the user hit it right after the "Launching..." indicator shipped, which is what surfaced it. `App.LaunchGame`'s `_launcherWindow.WindowState = WindowState.Minimized` call lived inside the `if (process is not null) { ... }` branch — meaning it **only ever ran for directly-tracked exe launches**, never for Steam/Xbox shell launches (`Process.Start` returns `null` for `steam://rungameid/...`/`shell:appsFolder\...`, which is correct and already documented behavior — the bug was gating the minimize call on that too, when the two things are unrelated). Result: launching anything through Steam or the Xbox app left the fullscreen, `Topmost="True"` launcher sitting visually on top of the game that had just started, with no indication anything had gone wrong (the game likely did start, just hidden behind the launcher). Fixed by moving the minimize call to run unconditionally, immediately after `Process.Start`, before the null check — every launch path now gets the same "get out of the way" behavior regardless of whether there's a `Process` to track afterward. **Not hands-on verified against a real Steam/Xbox launch** — no such install available in this environment.

## "Launching..." tile indicator (2026-08-08)

User reported clicking a game repeatedly because nothing indicated a launch had registered — worst for shell launches (Steam/Xbox), where `Process.Start` returns `null` immediately and the real game window can take several seconds to appear, with zero feedback in between. `GameTileViewModel.IsLaunching` (new `[ObservableProperty]`) drives a darkened full-tile overlay + "Launching..." label, added to both `GameTileStyle` and `RecentTileStyle` in `MainWindow.xaml`.

All of this routes through `App.LaunchGame` — already the single launch entry point every trigger path (gamepad Confirm, Enter/Space, double-click) funnels through, per the original single-instance/tray-core design — so this needed exactly one guard, not one per caller: `if (game.IsLaunching) return;` right at the top answers "repeat clicks" directly. Clearing the flag is based on what's actually knowable: a real `Process` back from `Process.Start` means the OS genuinely launched it, so it clears immediately (no guessing needed); a shell launch (`Process` is `null`) has no such signal at all, so it clears after a fixed `ShellLaunchIndicatorTimeout` (6s) instead — a real, if imprecise, tradeoff, not a bug: there's no reliable way to know when a `steam://`/`shell:appsFolder\...` launch actually produced a visible window, this app has hit that exact wall before (see the in-game-overlay section above, "known limitation, not a bug to fix casually").

**Hit a real XAML forward-reference crash while wiring this up**: `BoolToVisibility` (and the other two converters) were declared in `Window.Resources` *after* `GameTileStyle`/`RecentTileStyle`, and the new overlay's `Visibility` binding inside those styles' `ControlTemplate`s references `BoolToVisibility` via `StaticResource` — which, unlike `DynamicResource`, requires the resource to already be defined **earlier in the same `ResourceDictionary`**, not just present somewhere in it. First build attempt crashed on startup with `XamlParseException: Cannot find resource named 'BoolToVisibility'`. Fixed by moving all three converter declarations (`BoolToVisibility`/`InverseBoolToVisibility`/`EnumToBoolean`) to the very top of `Window.Resources`, before every `Style` that might reference them. **If a future `Style`'s `ControlTemplate` needs a converter via `StaticResource`, declare the converter before that `Style` in document order — this exact mistake is easy to repeat.**

**Not visually verified** — no screenshot capability here. Worth checking by hand: the overlay actually shows/hides at the right moments, and whether 6 seconds feels right for the shell-launch case (it's an unverified guess, not a measured value).

## Working agreements

- This is a solo commercial closed-source project (per README license section) — no public examples, no attribution needed in commits/code.
- When you finish a task, update `progress.md`'s checklist and log table. If you find a production-readiness gap, add it to `production-readiness.md` rather than fixing it silently mid-feature-work (unless trivial).
