# Context for Agents

Read this before working on Cartridge OS. Also read `README.md` (product vision/roadmap), `progress.md` (what's done), and `production-readiness.md` (what's left for shipping).

## What this is

A Windows game launcher: console-dashboard style, fullscreen, controller-first, detects games from every major PC launcher (Steam, Epic, GOG, etc.) plus manual additions. No accounts, fully offline.

## Current repo state

3-project solution (`CartridgeOS.sln`) — `CartridgeOS.Tray` was merged into `CartridgeOS.Launcher` on 2026-08-07 (see the single-instance/tray-core section below) and deleted; don't recreate it, and treat any reference to a separate Tray exe elsewhere as stale unless it's specifically talking about history.
- `src/CartridgeOS.Core` — classlib, targets `net10.0-windows` (bumped from plain `net9.0` once registry access needed it — the whole app is Windows-only, no reason to pretend otherwise). Has `Models/Game.cs`, `Data/GameDatabase.cs` (SQLite, create-table-if-not-exists + AddGame/GetAllGames/UpdateLastPlayed), `ViewModelBase` (CommunityToolkit.Mvvm `ObservableObject`), and `Scanning/SteamVdf.cs` + `Scanning/SteamScanner.cs` (reads Steam's install path from the registry, walks `libraryfolders.vdf` + `appmanifest_*.acf`, games launch via `steam://rungameid/{appid}` — no exe-path resolution needed). No repository interface/abstraction — `GameDatabase` is used directly, add an interface only if a second implementation is ever needed. `SteamScanner` is **not verified against a real Steam install** (none on this dev machine) — only `SteamVdf`'s parsing logic is tested, via synthetic sample text in `Launcher/Services/SteamScannerSelfCheck.cs` (`--self-check-steam`). Also has `Scanning/EpicManifest.cs` + `Scanning/EpicScanner.cs` (reads `%ProgramData%\Epic\EpicGamesLauncher\Data\Manifests\*.item`, JSON via `System.Text.Json`) — gives a direct exe path (unlike Steam's protocol-URI launch) but no artwork source yet, `ArtworkPath` always null. Also **not verified against a real install**; check via `--self-check-epic`. Plus GOG/Ubisoft/EA/Battle.net via `Scanning/InstalledProgramsScanner.cs` (walks the Windows uninstall registry) + `Scanning/PublisherGameScanner.cs` (filters by `Publisher` keyword, one instance per launcher, no subclasses) + `Scanning/ExecutableHeuristics.cs` (guesses the exe as the biggest non-installer `.exe` in the install root — the uninstall registry never gives the exe directly). These four are **meaningfully less trustworthy** than Steam/Epic/Riot: there's no documented manifest to key off, so both the exact `Publisher` string per launcher and the exe-guessing heuristic are unverified assumptions. Plus Riot via `Scanning/RiotManifest.cs` + `RiotScanner.cs` (real documented `RiotClientInstalls.json`, more trustworthy than the four heuristic ones — games route through the Riot Client exe itself). `MainViewModel.ScanForGamesCommand` runs all 7 sources (Steam, Epic, Riot, GOG, Ubisoft, EA, Battle.net) through the shared `ImportScannedGames(IEnumerable<Game>)` dedupe/persist path — add new scanners there too, and to `ScanForGames()`'s `.Concat()` chain, rather than duplicating the merge logic or adding another header button (there's one "Scan for Games" button, not one per launcher — that got consolidated once the count hit 7). **Xbox/Microsoft Store is not implemented** — UWP/MSIX packages don't appear in the uninstall registry at all; would need `Get-AppxPackage`/`PackageManager` APIs, a genuinely different mechanism from every other scanner here.

There's also `Scanning/StandaloneExecutableScanner.cs` — finds games not registered with any launcher at all (the README's "standalone executables" feature) by scanning top-level `Program Files`/`Program Files (x86)` folders for a plausible main exe, blocking known launcher/vendor-hub folder names. This is a pure heuristic with no manifest or registry backing it, so unlike every other scanner it's **not** wired into auto-add — `MainViewModel.FindMoreGamesCommand` routes results through `ScanResultsWindow`/`ScanResultsViewModel`/`CandidateGameViewModel` (a real confirmation dialog, checkboxes default-checked) before anything reaches `ImportScannedGames`. If you add more heuristic (non-manifest) scanners, follow this same "confirm before adding" pattern rather than wiring them into `ScanForGamesCommand`. `CandidateGameViewModel.IsSelected` defaults to `false` — user opts into what's actually a game rather than opting out of false positives (changed from an initial default of `true`). `ScanResultsViewModel` has `SelectAllCommand`/`UnselectAllCommand` for bulk toggling in the dialog. Note: this scanner has no concept of DRM/license status — a cracked exe and a legitimately-purchased DRM-free game look identical to it, by design.

`Scanning/StandaloneExecutableScanner.cs`'s `DefaultScanRoots()` covers more than just the system drive: `Program Files`/`Program Files (x86)` on the system drive, plus `Program Files` and `Games` on every other fixed drive (`DriveInfo.GetDrives()`, `DriveType.Fixed`). Steam/Epic/Riot never needed this fix — they already read real install paths from registry/manifests, which work on any drive by construction.

`Scanning/ArtworkFetcher.cs` — downloads box art once for games with no local `ArtworkPath` and caches it to `%LocalAppData%\CartridgeOS\ArtworkCache\downloaded\{title}_{id}.jpg` (never re-fetched once cached). Steam games (appid parsed back out of the `steam://rungameid/{appid}` exe path) hit Valve's public CDN directly, no API key. Everything else is looked up by title, trying two sources in order: **SteamGridDB first**, **TheGamesDB as fallback** (capped at 1000 requests/month — exactly why it's not primary) — whichever returns null (no match, error, rate-limited) falls through to the next. **Both API keys are hardcoded constants** (`SteamGridDbApiKey`/`TheGamesDbApiKey` at the top of the file) — deliberately not env vars: this is closed-source, the key ships inside the compiled DLL of every install regardless of whether it's a literal or read from an env var, so an env var buys no real protection while adding a setup step that would silently break artwork for every new install. Same reasoning as the Discord Client ID in `DiscordRichPresence.cs` — if you ever open-source this repo, revisit all three. A private nested `RateLimiter` class tracks a per-source cooldown after an HTTP 429 (`Retry-After` header parsed, defaults to 60s if absent) — shared static state across every concurrent fetch in the process, since games are fetched fire-and-forget in a loop after a scan and a burst of many new games would otherwise hammer a source that just rate-limited us. While a source is cooling down it's skipped entirely (falls through immediately), never retried inline/blocking. Logs every step (cache hit, chosen source, non-2xx bodies, 429/backoff, download success/failure) to `%LocalAppData%\CartridgeOS\artwork.log`, plain append, no rotation. **Known weak spot**: both sources' title matching takes the first search result with no disambiguation, can pick the wrong regional/platform/remaster variant. Wired into `MainViewModel` in two places: `ImportScannedGames` (real scan results) and the constructor's initial DB load (so seed/demo games get artwork too) — both fire-and-forget per game via `GameDatabase.UpdateArtworkPath` + `GameTileViewModel.SetArtworkPath` hot-swapping the tile once art lands, never blocking the UI. `SteamScanner.FindArtwork` also got a bug fix here: was only checking the old flat `{appid}_library_600x900.jpg` filename, now checks the newer per-appid nested path (`librarycache/{appid}/library_600x900.jpg`) first — that's why Steam art wasn't showing even with a real local Steam install.

Also `Scanning/XboxScanner.cs` — Xbox app/PC Game Pass/Microsoft Store games. These are UWP/MSIX packages with no registry entry and no simple manifest file (unlike every other scanner), so this shells out to PowerShell's `Get-AppxPackage` (user picked this over native WinRT `PackageManager` interop or a narrower XboxGames-folder-only scan — see conversation history if the tradeoff needs revisiting) rather than reimplementing package-manager logic. Games launch via `shell:appsFolder\{PackageFamilyName}!{AppId}`. Same trust model as `StandaloneExecutableScanner` — no reliable "is this actually a game" signal, so it also routes through `ScanResultsWindow` confirmation, merged together in `MainViewModel.FindMoreGamesAsync` (now genuinely async — spawning PowerShell can take a couple seconds, and this used to run synchronously on the UI thread before Xbox scanning was added, which would now freeze the window). **Not tested against real Xbox/Store games** — by explicit user choice (no such install available), not just an oversight; only self-check-verified.

`MainViewModel` also runs a `DispatcherTimer` (15-minute interval, `ScanTrustedLauncherSources()`) that re-scans the 7 trusted launcher sources in the background — `RescanInBackgroundAsync` wraps the scan in `Task.Run` so it doesn't hitch the UI thread, merges back via the same `ImportScannedGames`. Deliberately excludes `StandaloneExecutableScanner` (would pop a confirmation dialog unprompted). `MainWindow`'s `Closed` handler calls `StopBackgroundRescanning()` — don't forget that if you ever add a second window/entry point that constructs a `MainViewModel`, or the timer outlives its owner.
- `src/CartridgeOS.Launcher` — WPF, borderless fullscreen shell (`WindowStyle=None`, maximized, topmost), net10.0-windows. Cursor is currently visible (`Cursor="None"` was removed — hiding it broke mouse testing with no way to toggle it back; restore once there's real input-mode detection, show on mouse move / hide on gamepad input). `MainWindow` explicitly calls `Activate()` + `GameGrid.Focus()` in `Loaded` — without it, a debugger-launched window (e.g. from Visual Studio) can end up visible but not receiving keyboard input due to Windows' foreground-lock. Has `ViewModels/MainViewModel.cs` + `GameTileViewModel.cs`, and a virtualized game tile grid in `MainWindow.xaml` (`ListBox` + `VirtualizingWrapPanel` NuGet pkg, ns `WpfToolkit.Controls`) with a scale+glow selection animation. `MainViewModel` currently seeds placeholder titles into the DB if empty — remove that once the V2 scanner exists. Artwork loading is wired: `Services/ArtworkCache.cs` decodes at a fixed pixel width and disk-caches under `%LocalAppData%\CartridgeOS\ArtworkCache\`, keyed by `md5(sourcePath)_{width}.png`; `GameTileViewModel` loads it async and the tile falls back to a placeholder icon if `Artwork` is null (layered `Image` over icon in XAML, no converter). Verified via a synthetic self-check (`--self-check-artwork` launch arg → `Services/ArtworkCacheSelfCheck.cs`, exit 0/1) — **not yet verified with real box art**, since there's no scanner or manual-add UI to attach an `ArtworkPath` to a game yet. If you touch `ArtworkCache`, watch for the WPF UI-thread deadlock we hit: any code that calls `.GetAwaiter().GetResult()` on the UI thread before the Dispatcher message loop is pumping (e.g. in `OnStartup`) will hang unless the awaited method uses `ConfigureAwait(false)` throughout. Controller nav is wired: `Input/XInput.cs` + `Input/GamepadWatcher.cs` (raw P/Invoke of `XInputGetState`, no SharpDX/Vortice dep) poll on a background thread and drive `SelectedGame` via dpad/left-stick, A launches the exe. Column count for up/down nav is derived from `ListBox.ActualWidth` in `MainWindow.xaml.cs` at nav time, not a formal layout abstraction. **Not yet tested with a physical controller** — only verified the not-connected code path doesn't throw. Right-stick mouse emulation is also wired: `GamepadWatcher.RightStickMoved`/`RightTriggerChanged` (right trigger = click, not A — A already launches games) drive `Input/MouseEmulator.cs` (raw `user32.dll` P/Invoke, moves the real system cursor, primary monitor only). `GamepadWatcher.ApplyDeadzone` is `internal` (not `private`) specifically so `--self-check-mouse-emulation` can test the normalization math without hardware; that self-check also does a real (restored-afterward) cursor nudge. Same "not tested with a physical controller" caveat applies — the stick-to-cursor feel (speed constant `MaxPixelsPerTick` in `MouseEmulator.cs`) especially needs real-hands tuning. Has a Recent row (`RecentGames`, top 10 by `LastPlayedUtc` desc, collapses via `HasRecentGames`/`BooleanToVisibilityConverter` when empty) above the main grid, sharing `GameTileViewModel` instances with the main grid (same reference, just filtered/sorted — selecting in one reflects in the other). `MainViewModel.RecordPlayed(game)` is the single place that marks a game played (updates `LastPlayedUtc` + persists) and both launch paths (gamepad A, recent-tile double-click) route through the shared `LaunchSelected(vm, game)` helper in `MainWindow.xaml.cs`, which as of 2026-08-07 just forwards to `((App)Application.Current).LaunchGame(vm, game)` — the actual process-start/tracking logic lives in `App` now, see the single-instance/tray-core section below for why. `MainWindow.xaml` has a PS5-menu-style backdrop, and a collapsible settings sidebar as of 2026-08-07 with the first real settings option. Two full-window background `Image`s (`Grid.ColumnSpan="2"` now that the window is split into a content column + sidebar column, `IsHitTestVisible="False"`, dark gradient overlay on top for readability): `BackgroundArt` (bound to `SelectedGame.Artwork`) and `CustomBackgroundArt` (bound to `MainViewModel.CustomWallpaperImage`), toggled via `IsUsingGameArtworkBackground`/`InverseBooleanToVisibilityConverter` (`Converters/InverseBooleanToVisibilityConverter.cs`). Both blurred at `BlurEffect Radius=20` (was 60, cut after user feedback), `Opacity=0.85`, `Stretch="UniformToFill"` (tried `Uniform`/letterboxed briefly, user wanted full-screen fill back). `MainWindow.xaml.cs` fades whichever image actually changed back in (0→0.85, 250ms) via `MainViewModel.PropertyChanged` on `SelectedGame` or `CustomWallpaperImage` — not a true two-image crossfade, see progress.md to-dos.

Settings: `Core/Models/AppSettings.cs` (`WallpaperMode` enum, `CustomWallpaperPath`) + `Core/Data/SettingsStore.cs` persist to a plain JSON file at `%LocalAppData%\CartridgeOS\settings.json` (deliberately not a SQLite table — too small a shape to justify it). `MainViewModel.IsSettingsOpen`/`ToggleSettingsCommand` drive a right-side panel (gear-icon header button, `Border` in the window's sidebar column, plain `Visibility` toggle — no slide-in animation yet). `WallpaperMode` and `ChooseWallpaperCommand` (file picker → `ArtworkCache.LoadAsync` at 1920px, same decode/cache infra artwork tiles use) are the only settings that exist so far. `Converters/EnumToBooleanConverter.cs` binds the two-`RadioButton` wallpaper-mode choice to the single `WallpaperMode` enum property.

**Panel layout (2026-08-07, user asked for Steam/NVIDIA-app-style nesting)**: the panel is a native `TabControl` restyled via `SettingsTabControlStyle` (custom template docks a fixed-width `TabPanel` left as the category rail, content right — no new dependency) and `SettingsTabItemStyle` (category row styling). Each category is a `TabItem` — "Wallpaper" is the only one today. **Add a new category by adding another `TabItem Header="..."` inside the same `TabControl`** (see the comment at that XAML block) and give its content a `SettingsSectionHeadingStyle` heading, same shape as Wallpaper's. Sidebar `Border` is 460px wide to fit the category rail + detail content.
- `src/CartridgeOS.Service` — default Worker Service template, no actual logic yet, targets `net10.0-windows` (bumped alongside Core)

Both Launcher and Service have a project reference to Core. Both projects target `net10.0-windows` (LTS, supported to ~Nov 2028) — matches the README from the start; briefly built against `net9.0`/`net9.0-windows` early on because that was the only SDK installed, bumped once the .NET 10 SDK was installed. `Microsoft.Data.Sqlite` (Core) pins its transitive `SQLitePCLRaw.lib.e_sqlite3` dependency to `2.1.12` directly — the version Sqlite 10.0.10 pulls by default (2.1.11) has a known high-severity CVE (GHSA-2m69-gcr7-jv3q); re-check this pin if `Microsoft.Data.Sqlite` is ever upgraded, a newer release may fix it upstream and make the override unnecessary. Treat any claim of features beyond this as false until verified by reading the actual files.

## Tech decisions (already made — don't re-litigate)

- **UI framework: WPF**, not WinUI 3/Avalonia as the README's table originally suggested. Reason: user only has WPF available in their environment; WPF is sufficiently capable for a modern, fluid, hardware-accelerated fullscreen UI when paired with `WPF-UI` or `ModernWpfUI` for Fluent styling. See conversation history / progress.md for the reasoning.
- **Architecture per README's original diagram**: Windows Service (controller manager, input mapper, game scanner, config manager, IPC server) <-> Tray app <-> Fullscreen Launcher UI, talking over named pipes. **This has since diverged in one specific way (2026-08-07)**: Tray and Launcher were merged into one process (`CartridgeOS.Launcher.exe`) per explicit user request for Steam-style single-instance/tray-core/disposable-UI behavior — see the dedicated section below. The Service half of the diagram (controller manager, scanner, etc. living in a Windows Service) still hasn't happened; scanning/controller input still run inside Launcher. Don't "fix" the Tray/Launcher merge back apart without checking with the user first — it was deliberate, not drift.
- **Solution structure (current)**: `CartridgeOS.Core` (models, SQLite, scanning logic, IPC contracts), `CartridgeOS.Service`, `CartridgeOS.Launcher` (tray core + launcher UI, single process).
- **Data**: SQLite via `Microsoft.Data.Sqlite`, no ORM.
- **MVVM**: `CommunityToolkit.Mvvm` (source-generated, avoid handwritten boilerplate).
- **Controller input**: XInput via a thin wrapper (`Vortice.XInput` or `SharpDX.XInput`), polled on a background thread.

## Efficiency constraints (non-negotiable, per user's "must not be slow" requirement)

- Game tile grid MUST be virtualized (`VirtualizingWrapPanel` NuGet, not a plain `WrapPanel`) — libraries can have hundreds of games.
- Artwork must be decoded at target size (`BitmapImage.DecodePixelWidth`) and disk-cached, never loaded full-res per tile.
- Animate only `RenderTransform`/opacity (GPU-composited), never layout properties (`Width`/`Height`/`Margin`).
- All scanning/file IO off the UI thread.

## Roadmap phases (see progress.md for checklist state)

V1: fullscreen launcher, manual games, controller nav. V2: auto-detection across launchers, custom themes, sound. V3: overlay, suspend/resume, Discord RPC, cloud sync, plugins.

## Color scheme (locked — this is the app's theme, not a suggestion)

Built around the logo's dark charcoal background, white typography, and cyan-to-blue gradient. Implemented as WPF resource brushes in `src/CartridgeOS.Launcher/Theme.xaml` — use those `{StaticResource ...}` brushes in XAML, never hardcode these hex values inline.

**Primary brand:** Primary Cyan `#51E7ED` (symbol + "OS", used in the logo), Primary Blue `#49C1ED` (lower gradient blue), Deep Blue Accent `#168DDC` (stronger focus/selection state). Brand Gradient (logo, selected tiles, progress bars): `#51E7ED → #168DDC`.

**Dark interface:** App Background `#0D1016` (main fullscreen bg), Surface `#14171D` (nav bars, large panels), Raised Surface `#1B2028` (cards, menus, overlays), Hover Surface `#242B35` (hovered/focused secondary item), Border `#303946` (subtle panel/tile borders), Overlay Backdrop `rgba(5,8,12,0.82)` (modal/in-game overlay backdrop).

**Text:** Primary Text `#FBFBFB`, Secondary Text `#BCC1C9`, Muted Text `#767D88`, Text on Cyan `#071014` (text on bright buttons).

**Status (kept muted so they don't compete with brand cyan):** Success `#45D69A`, Warning `#F3B84B`, Error `#F05D68`, Information `#49C1ED`.

**Recommended UI usage:**
- Primary button: bg `#51E7ED`, hover `#70F1F3`, pressed `#39BFD2`, text `#071014`.
- Selected game tile: border `#49C1ED`, glow `rgba(73,193,237,0.35)`, background accent `rgba(81,231,237,0.08)`.
- Focused controller-nav element: focus ring `#51E7ED`, outer glow `rgba(81,231,237,0.30)`, ring thickness 3-4px.
- Navigation: active icon `#51E7ED`, inactive icon `#BCC1C9`, disabled icon `#5D6570`, active indicator gradient `#51E7ED → #168DDC`.

Implemented in `src/CartridgeOS.Launcher/Theme.xaml` (merged into `App.xaml`) and applied throughout `MainWindow.xaml` — always use the `{StaticResource ...}` brushes (`BackgroundBrush`, `SurfaceRaisedBrush`, `SurfaceHoverBrush`, `TextPrimaryBrush`, `BrandCyanBrush`, `BrandBlueBrush`, `PrimaryButtonStyle`, etc.), never hardcode these hex values again.

## Single-instance launcher, tray core, disposable UI (2026-08-07)

Built per an explicit, detailed user spec: single-instance app, a persistent lightweight tray/core half and a disposable launcher-UI half, memory released when the UI closes. This is the reason `CartridgeOS.Tray` no longer exists as a separate project — it's now `App.xaml.cs` inside `CartridgeOS.Launcher`.

**Single instance**: `App.OnStartup` takes a `Mutex(@"Global\CartridgeOS.SingleInstance")`. If another process already holds it, this process sends a `"ShowLauncher"` request over a dedicated named pipe (`CartridgeOsPipeServer`/`Client`, pipe name `"CartridgeOS.Launcher.SingleInstance"` — **not** the same pipe as the Service's IPC below, `CartridgeOsPipeServer`'s pipe name is now a constructor parameter defaulting to `CartridgeOsPipeServer.DefaultPipeName` so both can coexist) and exits immediately (`Environment.Exit(0)`) — no window, no tray icon, nothing else happens in that process. The primary instance's `HandleSingleInstanceSignal` dispatches the request onto the UI thread and calls `ShowLauncher()`.

**Core vs disposable UI**: `App` owns everything that must survive the launcher window being closed — `GamepadWatcher`, `MouseEmulator`, `GlobalHotkey`, `DiscordRichPresence`, running-game process tracking, and the whole overlay flow (`ToggleOverlay`/`ReturnToLauncher`/`QuitRunningGame`, moved here from `MainWindow.xaml.cs`). `GlobalHotkey` needs a real native window handle to register `RegisterHotKey` against; since the launcher window can be destroyed at any time, `App` creates a small always-alive, never-shown `_coreWindow` (`Width=0 Height=0 Visibility=Hidden`) purely for this, via `WindowInteropHelper.EnsureHandle()` (not `.Handle`, which returns zero for a window that's never been shown — `GlobalHotkey.cs` was changed to use `EnsureHandle()` to support this). Gamepad button routing also moved to `App`: it subscribes to `GamepadWatcher.ButtonPressed` once, handles `Start` (toggle overlay) itself, and forwards everything else to `_launcherWindow?.HandleGamepadButton(button)` when a window exists.

`MainWindow.xaml.cs` is now genuinely disposable: no long-lived service references of its own. `App.ShowLauncher()` constructs it (passing the last-selected game's id so it can restore the selection) when `_launcherWindow` is null, or just restores/activates the existing one otherwise — this single method is the answer to "open the launcher" from every caller: initial startup, tray icon click/double-click, the single-instance pipe signal, the overlay's "Return to Cartridge OS" button, and auto-reopen on game exit. `App.OnLauncherClosed()` (wired to the window's `Closed` event, which fires for the X button, Alt+F4, or a programmatic `Close()` — `ShutdownMode="OnExplicitShutdown"` in `App.xaml` means none of those quit the app) captures `_lastSelectedGameId`, drops the window reference, then does one deliberate `GC.Collect()` + `GC.WaitForPendingFinalizers()` pass. That GC call is intentional, not an accident to clean up later: WPF `BitmapImage` isn't `IDisposable`, so there's no per-object dispose loop to write for the freed artwork/decoded images — dropping every reference to the closed window/viewmodel/`GameTileViewModel` graph and forcing a collection *is* the correct way to reclaim that memory promptly instead of waiting for whatever GC happens to run next.

`LaunchGame` (moved off `MainWindow.LaunchSelected`, which now just forwards to `((App)Application.Current).LaunchGame(vm, game)`) starts the process, sets Discord presence, and — **user's explicit choice**: minimizes rather than destroys the launcher window on launch (cheaper, no recreate-flicker; the window can still be fully closed independently via the X button while a game runs, same `OnLauncherClosed` path as any other close) — tracks the process for the overlay, and on exit (`OnGameExited`) clears Discord presence and calls `ShowLauncher()` again automatically (**user's explicit choice**: Steam-like auto-reopen, not stay-closed).

Tray context menu is now just **Open Cartridge OS** / **Exit Cartridge OS** — the old Tray project's "Service Status" item was dropped rather than ported, since mixing the separate/still-unused Service's status into this merged app's menu didn't make sense once Tray and Launcher were the same process. `ExitApplication()` (wired to "Exit Cartridge OS") is the only path that actually calls `Shutdown()` — it cancels the single-instance pipe server, stops the gamepad poll loop, disposes the hotkey/Discord connection, closes the overlay and launcher window, disposes the tray icon, closes the core window, and releases+disposes the mutex, in that order.

**Verified hands-on** (via PowerShell + raw Win32 calls, working around this environment's UI-automation limitation — see below): started the app (one process) → ran the exe again (second process exited immediately, code 0, no duplicate) → sent `WM_CLOSE` via `PostMessage` to the real window handle found through `EnumWindows`/`GetWindowThreadProcessId` (`Get-Process.MainWindowHandle` came back `0` for this borderless/topmost window and couldn't be used directly) → confirmed the window closed but the same PID kept running → ran the exe again → confirmed the *same* process's window reopened. All four core requirements confirmed working, not just compiling. **Not verified by hand**: tray icon visual appearance/context-menu clicks (can't click a real tray icon from here), and the overlay/Discord-presence flow during an actual running game.

## Named-pipe IPC (Service <-> Launcher, and Launcher <-> Launcher for single-instance)

`Core/Ipc/` has a real, working named-pipe transport: `PipeFraming.cs` (length-prefixed UTF-8 framing), `PipeMessages.cs` (`PipeRequest`/`PipeResponse` records), `CartridgeOsPipeServer.cs` (one connection handles exactly one request then closes — not a long-lived duplex session; `pipeName` is a constructor parameter, defaults to `DefaultPipeName`), `CartridgeOsPipeClient.cs` (`SendAsync`, also takes an optional `pipeName`, returns `null` on any failure — nothing listening, timeout, busy — never throws). All IPC classes use `ConfigureAwait(false)` throughout; don't remove it, see the WPF-deadlock note above — the same class of bug bit this code too, before it ever shipped.

`CartridgeOS.Service`'s `Worker.cs` hosts a server on the default pipe name, backed by the actual shared `GameDatabase` (same `%LocalAppData%\CartridgeOS\games.db` the Launcher uses) — `Ping` and `GetGameCount` are real commands, not stubs, reachable via `--ipc-ping` from the Launcher. **Separately**, `App.xaml.cs` hosts a second server on its own pipe name (`"CartridgeOS.Launcher.SingleInstance"`) purely for single-instance signaling — see the section above. These two servers are unrelated; don't conflate them.

**Scope note — the transport is real, the Service-side architecture migration is not.** The README's architecture diagram shows Controller Manager/Input Mapper/Game Scanner/Configuration Manager all living in the Service, with the Launcher talking to them over this pipe. That has **not** happened — scanning (`Core/Scanning/*`) and controller input (`Input/GamepadWatcher.cs`, now owned by `App`) still run directly inside the Launcher process. Migrating that ownership into the Service is a much bigger, separate piece of work (would need to redesign how the Launcher's UI stays in sync with Service-owned state — polling? a persistent duplex connection instead of one-request-per-connection? push notifications on scan completion?) and hasn't been scoped yet. If you pick that up, decide on the sync model first — the current one-shot request/response server as built doesn't support server-initiated push, only client-initiated pull.

Testing note: `Microsoft.Data.Sqlite` pools connections by default — `Dispose()` doesn't release the underlying file handle. Any code (tests, scripts) that creates a temp SQLite file and needs to delete it afterward must call `Microsoft.Data.Sqlite.SqliteConnection.ClearAllPools()` first, or the delete will fail with a sharing violation. `Services/PipeIpcSelfCheck.cs` in Launcher hit this.

## Sound effects

`Services/SoundService.cs` plays `Assets/Sounds/nav.wav` (grid navigation) and `confirm.wav` (game launch) via `System.Media.SoundPlayer`. Both files are **procedurally-generated placeholder tones** (raw PCM sine waves, synthesized via a one-off PowerShell script — not build-integrated), not real sound design — same "derive something usable now, swap later" approach used for the app icon before the logo was available, except here there's no source asset at all, it's fully synthetic. Marked `Content`/`CopyToOutputDirectory` in the Launcher `.csproj` (not `Resource` — `SoundPlayer` needs a real file path). Verified via `--self-check-sound` that the files exist and load without error; **never confirmed anything is actually audible** — no way to check that from this environment.

## Branding assets

`assets/logo/` (repo root, outside any project) has the source files: `logo symbol.png`, `logo with name.png` (both 1536x1024, genuinely alpha-transparent — a viewer showing them as flat gray is just rendering the transparency, not proof the file lacks it — verify with pixel alpha before assuming otherwise), and `cartridge.svg`. `src/CartridgeOS.Launcher/Assets/` has derived files actually used by the app: `app-icon.png` (256x256, cropped/resized from the symbol) and `app.ico`, generated by a one-off PowerShell + `System.Drawing` script (not part of the build — if the source logo changes, redo the crop/resize/ico-export manually) and `logo-with-name.png` — **not** a straight copy anymore: the source `assets/logo/logo with name.png` is a 1536x1024 canvas with a lot of transparent padding around a much smaller logo+text lockup (real content bounds found via alpha-channel scan: roughly x=204-1316, y=338-610), so the Launcher's copy is cropped to that content region (+20px padding) before use — otherwise `Stretch="Uniform"` wastes most of the display height on empty space, which is exactly what made the header logo unreadably small before this was caught. If the source logo changes again, recrop, don't just copy. Tray and Service don't have branding wired yet.

**If the user says they edited a logo file and the app doesn't reflect it**: the `assets/logo/` source files are NOT what the app reads at runtime — WPF `Resource` items embed into the compiled assembly from `src/CartridgeOS.Launcher/Assets/*.png` at build time. Editing only `assets/logo/...` does nothing until that file is re-copied (and, for `logo-with-name.png`, recropped — see above) into `src/CartridgeOS.Launcher/Assets/` and the project rebuilt. This already happened once (`logo-with-name.png`) — check both locations' file sizes/timestamps before assuming a build/cache problem. To verify the fix actually took (not just "it built without error"), extract the embedded manifest resource straight from the compiled DLL and check its byte length matches the new source file — `Assembly.LoadFrom(dllPath).GetManifestResourceStream(...)` via a `System.Resources.ResourceReader` over the `*.g.resources` entry, resource key is the lowercased relative path (e.g. `assets/logo-with-name.png`).

## In-game overlay (Phase 3, started 2026-08-07; moved from MainWindow to App on 2026-08-07)

`Input/GlobalHotkey.cs` — raw `RegisterHotKey`/`WM_HOTKEY` P/Invoke, `Ctrl+Shift+O`, fires even without window focus. Originally registered against `MainWindow`'s handle; **now registered against `App`'s always-alive hidden `_coreWindow`** (see the single-instance/tray-core section above) so the hotkey keeps working even when the launcher window is closed. `OverlayWindow.xaml` + `ViewModels/OverlayViewModel.cs` — borderless, `Topmost`, docked bottom-right via `SystemParameters.WorkArea`, shows game title + Return/Quit buttons. `App` owns the `OverlayWindow` instance and the whole `ToggleOverlay`/`ReturnToLauncher`/`QuitRunningGame` flow now, not `MainWindow`.

`App.LaunchGame` (moved off `MainWindow.LaunchSelected`) tracks the launched `Process`, hooks `Exited`, and minimizes the launcher window on launch (user's explicit choice, not full destroy — see above) so it doesn't sit visually on top of the game (`MainWindow` is `Topmost="True"`, set back in V1 for the kiosk look). `App.OnGameExited` (dispatched from `Process.Exited`, which fires on a threadpool thread) now calls `ShowLauncher()` (auto-reopen, user's choice) rather than just restoring window state, since the window may have been closed entirely while the game was running.

**Known limitation, not a bug to fix casually**: `Process.Start(UseShellExecute: true)` returns `null` for shell-protocol launches — `steam://rungameid/{id}` (every Steam game) and `shell:appsFolder\...` (Xbox/Store games) — so there's no `Process` to attach `Exited` to, and the overlay/hotkey does nothing for those today. Only directly-launched `.exe` paths (manual add, standalone-scanner finds, Epic, the four `PublisherGameScanner` launchers, Riot's client exe) get overlay support. If this needs fixing for Steam specifically, the general approach is polling for a plausible child process by name/window rather than trusting `Process.Start`'s return value — not attempted here, scope it as its own task if wanted.

Controller toggle: `GamepadButton.Start` (Xbox "Menu"/PS "Options") calls `ToggleOverlay()` too — `App.OnGamepadButton` intercepts `Start` itself rather than forwarding it to the launcher window, so this works even with no launcher window open. Works without focus for free, since `GamepadWatcher` polls XInput on a background thread regardless of which window has focus — unlike the keyboard hotkey, which needed `RegisterHotKey` specifically to bypass that requirement.

**Not implemented**: overlay likely won't render on top of DirectX-exclusive-fullscreen games despite `Topmost` — real overlays (Discord/Steam) solve this via graphics-API hooking, genuinely out of scope for a `Topmost` WPF window.

## Discord Rich Presence (Phase 3, started 2026-08-07; moved from MainWindow to App on 2026-08-07)

`Services/DiscordRichPresence.cs` talks to Discord's local IPC pipe directly (`\\.\pipe\discord-ipc-{0..9}`, handshake + `SET_ACTIVITY` frames) — no `discord-rpc` dependency. `ClientId` is the real registered Application ID (`1535179486117502976`, not a secret — shared by every Cartridge OS install).

Now owned by `App` (was `MainWindow`) so it survives the launcher window being closed: connects in `App.OnStartup` (fire-and-forget, silently no-ops if Discord isn't running), sets activity right after every game launch via `App.LaunchGame` — including Steam/Xbox shell-launches, since setting activity doesn't need the `Process` handle the way overlay tracking does — and clears it in `App.OnGameExited`, which is only reachable for the subset of launches that do produce a trackable `Process` (see the overlay section above for which those are). So: Steam/Xbox presence will show "Playing X" but won't auto-clear when that game closes, since we have no signal for when it does.

Suspend/Resume (the Phase 3 item before this one) was evaluated and explicitly **not built** — see the 2026-08-07 progress.md log entry for the full reasoning (real console-style suspend isn't possible on Windows for arbitrary processes; the only achievable version is freezing threads via undocumented `NtSuspendProcess`, which risks anti-cheat bans in online games). Don't pick this back up without a narrower, safer scope than "suspend any game."

## Environment limitation: no real UI automation here

This session cannot drive interactive input against a running WPF window and get trustworthy results. `[Microsoft.VisualBasic.Interaction]::AppActivate` / `SendKeys` appear to run without error, but `GetForegroundWindow()` confirms the target window never actually gains focus — keystrokes go nowhere, and a "nothing happened" result from that approach is not evidence of a bug. If you need to verify actual keyboard/mouse/gamepad behavior, say so explicitly and ask the user to test by hand rather than trying to automate it from here again.

**What *does* work from here, found 2026-08-07**: sending real Win32 messages to a window handle doesn't need focus, only a valid `hWnd` — that's a genuinely different mechanism from `SendKeys`/`AppActivate`, which simulate input at the focused-window level. `Get-Process.MainWindowHandle` is unreliable for a borderless/topmost WPF window (came back `0` even for a window that was definitely visible) — instead enumerate real windows via P/Invoke `EnumWindows` + `GetWindowThreadProcessId` filtered to the target PID, then `PostMessage(hwnd, WM_CLOSE=0x0010, 0, 0)` to close it, or similar messages for other verifiable state changes. This is how the single-instance/disposable-launcher-UI work above was actually verified end-to-end (process survives window close, etc.) rather than just left as "builds clean." Doesn't help with keyboard/mouse/gamepad *input* simulation, only with driving a window through documented Win32 messages it already responds to (close, etc.).

## Working agreements

- This is a solo commercial closed-source project (per README license section) — no public examples, no attribution needed in commits/code.
- When you finish a task, update `progress.md`'s checklist and log table. If you find a production-readiness gap, add it to `production-readiness.md` rather than fixing it silently mid-feature-work (unless trivial).
