# Progress

Tracks what's actually done. Update this whenever a task is finished — don't batch.

## Status: Phase 1 (V1) functionally complete

Fullscreen launcher with controller nav, keyboard/mouse fallback, manual add-game, artwork loading, recent games row, PS5-style carousel with two-tab interface, tray icon, and brand theming are all built and hand-verified. Only the Fluent-theming checklist item is deliberately deferred. Phase 2 (auto-detection scanners) not started.

## Log

| Date | What was done |
|---|---|
| 2026-08-06 | Repo has default WPF template only. Wrote implementation plan (see context.md). Created tracking files. |
| 2026-08-06 | Scaffolded 4-project split: `CartridgeOS.sln` + `CartridgeOS.Core` (classlib), `CartridgeOS.Launcher` (WPF, borderless fullscreen shell), `CartridgeOS.Tray` (WPF), `CartridgeOS.Service` (worker). Launcher/Tray/Service all reference Core. Targets `net9.0`/`net9.0-windows` (only .NET 9 SDK installed locally; README calls for .NET 10 — bump when that SDK is available). Deleted the old single-project template. Builds clean (`dotnet build`, 0 warnings/errors). |
| 2026-08-06 | Added `CommunityToolkit.Mvvm` and `Microsoft.Data.Sqlite` to Core. Added `Models/Game.cs`, `Data/GameDatabase.cs` (create-table-if-not-exists + AddGame/GetAllGames, no repository interface — only one implementation), and `ViewModelBase` (thin `ObservableObject` subclass) for Launcher/Tray to share. Builds clean. |
| 2026-08-06 | Built the game tile grid in Launcher: `MainViewModel`/`GameTileViewModel` (loads from `%LocalAppData%\CartridgeOS\games.db`, seeds 8 placeholder titles if empty — ponytail-marked, delete once the V2 scanner exists), `ListBox` + `VirtualizingWrapPanel` (NuGet `VirtualizingWrapPanel` pkg, namespace `WpfToolkit.Controls`) for 2D-virtualized tiles, selection-driven scale + border-glow `Storyboard` animation on `ListBoxItem.IsSelected`. Verified it builds and runs without crashing (process alive, no errors); did not visually confirm rendering/animation — no screenshot capability in this environment, eyeball it yourself. |
| 2026-08-06 | Built artwork loading: `Services/ArtworkCache.cs` decodes at a fixed pixel width (`BitmapImage.DecodePixelWidth`, never full-res) and disk-caches the decoded PNG under `%LocalAppData%\CartridgeOS\ArtworkCache\{md5(sourcePath)}_{width}.png`, keyed off source path + width, re-encoded only when the source is newer than the cache entry. `GameTileViewModel` loads artwork async in its constructor (fire-and-forget, exceptions swallowed to keep the placeholder tile on failure); tile XAML layers `Image` over the placeholder icon so a null `Artwork` falls back automatically, no converter/visibility binding needed. Added `Services/ArtworkCacheSelfCheck.cs` (generates a temp PNG, verifies decode-width/cache-hit/missing-file paths, exits 0/1) wired behind `--self-check-artwork` in `App.xaml.cs` — hit a classic WPF UI-thread deadlock running it (blocking `.GetResult()` on the UI thread before `Dispatcher.Run()` starts, while the awaited method's continuation tried to resume on that same not-yet-pumping thread); fixed with `ConfigureAwait(false)` inside `ArtworkCache.LoadAsync`. Self-check passes (exit 0); full app still starts cleanly. No real artwork sources exist yet (no scanner/manual-add UI), so the happy path is unexercised with real box art — verify visually once artwork is actually attached to a game. |
| 2026-08-06 | Manual add-game flow + logo integration. `MainViewModel.AddGameCommand` (plain `RelayCommand`, no source-gen) opens a `Microsoft.Win32.OpenFileDialog` for the exe, then an optional one for artwork, writes to `GameDatabase`, and appends a new tile live. Wired to a "+ Add Game" button in a new header row and to the gamepad Y button. Logo: user supplied `assets/logo/` (`logo symbol.png`, `logo with name.png`, `cartridge.svg`) — confirmed via pixel inspection these PNGs have real alpha transparency (not the flat gray the preview viewer shows). Cropped a square region around the mark (bbox found at x:528-988, y:208-760 in the 1536x1024 source) and generated `src/CartridgeOS.Launcher/Assets/app-icon.png` (256x256) + `app.ico` via a one-off PowerShell/System.Drawing script (not build-integrated — rerun manually if the source logo changes). Wired `app.ico` as `<ApplicationIcon>`, `app-icon.png` as `Window.Icon`, `logo-with-name.png` in the new header row. Builds clean, runs without crashing. Tray's icon set in the next entry below. |
| 2026-08-06 | Converted Tray from the default WPF window template to a real system-tray app: added `Hardcodet.NotifyIcon.Wpf`, deleted `MainWindow.xaml`/`.cs`, `TaskbarIcon` defined in `App.xaml` (`ShutdownMode="OnExplicitShutdown"` — required, since with zero windows the default `OnLastWindowClose` shuts the app down immediately on startup). Context menu: "Open Launcher" (also on double-click) and "Exit". `FindLauncherExe()` looks in its own directory first (assumes a real installer places every exe together), falls back to a dev-only sibling-bin-path guess (ponytail-marked, delete once there's an installer) so `dotnet run` works without manual copying during development. Copied `app-icon.png`/`app.ico` into `src/CartridgeOS.Tray/Assets/` (each project needs its own copy — pack URIs don't cross assemblies). Verified: builds clean, process stays alive with no window (confirms `ShutdownMode` is doing its job) and exits cleanly via `Exit_Click`/`OnExit` disposing the `TaskbarIcon`. Did not visually confirm the icon renders correctly in the notification area or that "Open Launcher" actually launches — no screenshot capability here, check both by hand. |
| 2026-08-06 | Wired the user-supplied brand color scheme as the app's actual theme (see context.md for the full palette). Added `Theme.xaml` (brand/background/text/status brushes + `PrimaryButtonStyle` with hover/pressed states) merged into `App.xaml`. Replaced every hardcoded hex in `MainWindow.xaml` with theme brushes: window background, tile surfaces, artwork-slot background, title text, selection-border animation color (now brand blue `#49C1ED` instead of the placeholder purple), Add Game button now uses `PrimaryButtonStyle`. Builds clean, runs without crashing. Skipped the spec's "selected tile background accent" tint (`rgba(81,231,237,0.08)`) — needs a per-item animatable brush instance instead of the shared frozen resource brush currently used, small enough to add later without restructuring. |
| 2026-08-06 | Built the recent-games row. `GameDatabase.UpdateLastPlayed(id, utc)` added. `GameTileViewModel.LastPlayedUtc` is now a mutable `[ObservableProperty]` with `MarkPlayedNow()`. `MainViewModel.RecordPlayed(game)` marks played, persists to DB, rebuilds `RecentGames` (top 10 by `LastPlayedUtc` desc); `HasRecentGames` drives a `BooleanToVisibilityConverter` binding so the row collapses entirely when nothing's been played yet. Wired into both launch paths (gamepad A, recent-tile double-click) via a shared `LaunchSelected(vm, game)`. Added a `RecentTileStyle` (smaller tiles, horizontal `StackPanel`, no virtualization — capped at 10 items, not worth it) reusing the same selection animation as the main grid. Seed data (`SeedIfEmpty`) now backdates `LastPlayedUtc` on 3 of the 8 placeholder titles so the row has something to show in dev, since seeded games have no real exe to actually launch. Cleared the dev DB and reran to confirm the seed change took effect and the row renders without crashing. |
| 2026-08-06 | Added the selected-tile background accent tint (`rgba(81,231,237,0.08)` per color scheme) to both `GameTileStyle` and `RecentTileStyle`. Confirmed the existing `BorderBrush` animation only worked per-tile because it's declared inline inside the `ControlTemplate` (not a shared `StaticResource`) — each `ListBoxItem` gets its own instance when the template is applied, so a `Storyboard.TargetName` animation on it doesn't leak across tiles. Followed the same pattern: added an inline `AccentTint` Border (opaque `#51E7ED`, `Opacity="0"`, `IsHitTestVisible="False"`, spans both rows) behind the artwork/title, animated `Opacity` 0↔0.08 alongside the existing scale/border-color animation on `IsSelected`. Builds clean, runs without crashing. |
| 2026-08-06 | Added keyboard equivalents of the gamepad A/Y buttons (Enter/Space → launch, Insert → add game) so the nav/launch/add-game flow is testable without a physical controller — arrow-key navigation already worked for free via native `ListBox`/`VirtualizingWrapPanel` behavior. Decided to defer WPF-UI/ModernWpfUI (Fluent theming checklist item): only two control types exist in the app right now (`Button`, `ListBox`), both already hand-templated to the brand palette — adding a Fluent library now means overriding its own styles to match anyway. Revisit once more control variety (settings, theme picker) is actually being built. |
| 2026-08-06 | Tested the keyboard flow end-to-end via `SendKeys` against the running Launcher (arrow-key nav + Enter to launch) — confirmed the process stays alive and responsive, no crashes or unhandled exceptions. Did not test the Insert/add-game shortcut this way (opens a native modal file dialog which can't be safely driven headlessly without risking a stuck dialog) — test that one by hand. Added a "Running & Testing" section to `README.md`: `dotnet run` commands per project, the gamepad-to-keyboard mapping table, the `--self-check-artwork` self-check, and pointers to `progress.md`/`production-readiness.md`/`context.md`. |
| 2026-08-06 | Fixed two usability issues reported testing in Visual Studio: (1) keyboard input wasn't reaching the window — Windows' foreground-lock can leave a debugger-launched window without keyboard focus even though it's visible, fixed by explicit `Activate()` + `GameGrid.Focus()` in the `Loaded` handler; (2) "mouse not working" was `Cursor="None"` making the pointer invisible (intentional kiosk design), not a functional bug — removed it for now since there's no way yet to tell controller-active vs mouse-active input mode. Revisit hiding the cursor once there's real input-mode detection (show on mouse move, hide on gamepad input — Steam Big Picture does this). |
| 2026-08-06 | Fixed keyboard Left/Right not navigating (Up/Down worked, per user report testing in VS): the native `ListBox`/`VirtualizingWrapPanel` arrow-key handling apparently only supports Up/Down correctly. Stopped relying on it — extracted the directional-move logic out of `OnGamepadButton` into a shared `HandleInput(vm, button)`, wired `PreviewKeyDown` (Left/Right/Up/Down/Enter/Space/Insert) to call it directly and set `e.Handled = true` so native handling can't also fire and double-move. Added `AutomationProperties.Name="{Binding Title}"` to both tile styles along the way (accessibility win, and needed it to identify tiles via UI Automation while investigating). Could not verify the fix moves the on-screen selection from this session — discovered mid-investigation that `AppActivate`/`SendKeys` silently fail to focus a spawned window here (`GetForegroundWindow()` never changes), so any prior "nothing happened" result from that approach was a false signal, not a real repro. The fix is sound by code inspection (same logic gamepad already used successfully); needs a hands-on retest in Visual Studio to confirm. |
| 2026-08-06 | User confirmed by hand in Visual Studio: keyboard (all four arrow directions) and mouse both work correctly now. Phase 1 is functionally complete except the deliberately-deferred Fluent theming item. |
| 2026-08-06 | Bumped all 4 projects from `net9.0`/`net9.0-windows` to `net10.0-windows` — user installed the .NET 10 SDK. .NET 9 (what we'd been targeting) is an 18-month STS release and already out of support as of today; .NET 10 is LTS (supported to ~Nov 2028) and matches what the README specified from the start. Restore surfaced two pre-existing issues, fixed while in here: `Microsoft.Win32.Registry` package removed from Core (redundant on `net10.0-windows` — registry access is built into the Windows-targeted framework now, NuGet flagged it "will not be pruned"); pinned `SQLitePCLRaw.lib.e_sqlite3` to `2.1.12` (Core) to close a known high-severity vulnerability (GHSA-2m69-gcr7-jv3q) in the version `Microsoft.Data.Sqlite` 10.0.10 pulls transitively — `Microsoft.Data.Sqlite` itself is already the latest stable, so the fix has to happen at the transitive-package level. Solution builds with 0 warnings/0 errors, both self-checks pass, full app still runs. |
| 2026-08-06 | Added a "User Guide" section to `README.md`, aimed at end users rather than developers (the existing "Running & Testing" section stays dev-focused — dotnet CLI, self-checks). Covers: starting the app (Tray vs Launcher directly), the three ways to add games, a simplified controller/keyboard/mouse table, the Recent row, tray icon menu, and a short known-limitations list cross-referencing `production-readiness.md`. Also fixed two stale README sections while in there: "Project Status" still said "design and planning phase" despite V1 + most of V2 being built; the Tech Stack table still listed "WinUI 3 or Avalonia UI" from before the WPF decision. |
| 2026-08-06 | Added Minimize and Close buttons to the header — user pushed back on Alt+F4 being the only way to exit a borderless fullscreen window. Two icon-only buttons (Segoe MDL2 Assets glyphs `E921`/`E8BB`, standard Windows minimize/close chrome glyphs) in a new third header column, `MinimizeButtonStyle`/`CloseButtonStyle` (close hovers red via `ErrorBrush`, matching the usual convention). `Minimize_Click` sets `WindowState.Minimized`, `Close_Click` calls `Close()`. Mouse/keyboard users now have a visible way out; **gamepad still doesn't** — no controller button is bound to exit/minimize yet, noted as still-open in `production-readiness.md`'s controller-only-navigation item rather than checked off. Builds clean, full app runs. |
| 2026-08-06 | Fixed the header logo's poor readability. Real cause wasn't just display size: `assets/logo/logo with name.png` is a 1536x1024 canvas but the actual logo+text lockup only occupies a ~1112x272 region within it (found via alpha-channel bounding-box scan, same technique used for the icon crop earlier) — at `Height="56"` with `Stretch="Uniform"` on the *uncropped* canvas, most of that height was being spent on transparent padding, not the visible logo. Cropped to the real content bounds (+20px padding) and re-saved into `src/CartridgeOS.Launcher/Assets/logo-with-name.png` (199,063 bytes, down from 2.1MB), bumped display `Height` from 40 to 56. Verified the embedded resource matches the new cropped file's byte count exactly (same DLL-resource-extraction check as last time — this is now the second time that check caught what "it built without error" alone wouldn't have). Builds clean, full app runs. |
| 2026-08-06 | Fixed a stale-asset bug and improved the Find More Games dialog. User updated `assets/logo/logo with name.png` (brighter/white text for visibility) but the app kept showing the old logo — root cause: `src/CartridgeOS.Launcher/Assets/logo-with-name.png` is a separate copy (WPF `Resource` items embed into the assembly, they don't read from `assets/logo/` at runtime), so editing the source file alone doesn't touch what's actually embedded. Re-copied the file and rebuilt; verified via extracting the embedded manifest resource straight from the compiled DLL and checking its byte length matches the new source exactly (2,106,143 bytes) — not just "it built without error." **This copy-then-forget-to-recopy trap will keep happening if the logo changes again** — worth remembering. `ScanResultsWindow`/`ScanResultsViewModel`: added Select All / Unselect All buttons (`SelectAllCommand`/`UnselectAllCommand`, plain `RelayCommand`s), and changed `CandidateGameViewModel.IsSelected` to default `false` instead of `true` — user now opts into what's actually a game rather than opting out of false positives, which is the safer default for an inherently noisy heuristic scan. Updated the dialog's instruction text to match ("check the ones that actually are games"). Builds clean, full app runs. |
| 2026-08-06 | Built the Xbox / Microsoft Store scanner. User decided the two open design questions: detection method = shell out to PowerShell `Get-AppxPackage` (not native WinRT `PackageManager` interop, not a narrower XboxGames-folder-only scan); test access = self-check only, no real Xbox/Store install available. `Core/Scanning/XboxScanner.cs` runs an embedded PowerShell script (`Get-AppxPackage` filtered to non-framework/non-resource/non-system packages, reads each package's `AppxManifest.xml` for display name + first Application Id), returns games that launch via `shell:appsFolder\{PackageFamilyName}!{AppId}` (Explorer's shell namespace, works with `Process.Start(UseShellExecute: true)` same as everything else). Same trust model as `StandaloneExecutableScanner`: no reliable "this is actually a game, not some other Store app" signal, so results route through the same confirmation dialog, not the trusted auto-add path — `MainViewModel.FindMoreGamesAsync` now scans both sources together. Made `FindMoreGames` properly async (`Task.Run` + `await`) since spawning PowerShell and waiting on it can take a couple seconds — running that synchronously on the UI thread like the standalone-only version did would have frozen the window. Added `--self-check-xbox`: verifies JSON parsing against both shapes `ConvertTo-Json` can emit (array for 2+ results, bare object for exactly 1 — an easy thing to get wrong), plus malformed/missing-field cases, **and** actually runs the real PowerShell script as a smoke test (no Store games installed here, so this only proves the script itself doesn't error — which is the highest-risk failure mode of this whole approach) — passes. Builds clean, full app runs. **Not tested with real Xbox/Store games** — by the user's own choice, matching the pattern already established for GOG/Ubisoft/EA/Battle.net/Riot/standalone. |
| 2026-08-06 | Built right-stick mouse emulation (V2 roadmap item). `Input/XInput.cs` gained `RightThumbDeadzone` (Microsoft-recommended value). `Input/GamepadWatcher.cs` now reads the right stick and right trigger every poll: `RightStickMoved(float, float)` fires with deadzone-filtered normalized (-1..1) values whenever off-center, `RightTriggerChanged(bool)` fires on press/release edges (used as the click button — A is already "launch game", so triggers avoid the clash) and force-releases on controller disconnect so the mouse button can't get stuck down. `ApplyDeadzone` made `internal` (not `private`) specifically so the self-check can verify the normalization math without needing real hardware. New `Input/MouseEmulator.cs` — raw `user32.dll` P/Invoke (`GetCursorPos`/`SetCursorPos`/`mouse_event`/`GetSystemMetrics`), moves the real system cursor by stick-deflection-scaled pixel deltas each tick (right-stick Y is inverted relative to screen Y — up on the stick is positive, down on screen is positive), clamped to the primary monitor only (no multi-monitor virtual-desktop handling yet). Wired directly into `MainWindow`'s existing gamepad event subscriptions — runs on the background poll thread, no `Dispatcher` marshaling needed since it's pure Win32 calls, not WPF objects. Added `--self-check-mouse-emulation`: verifies the deadzone math directly (in-deadzone → 0, full deflection → ±1), then does a real (but restored-afterward) cursor nudge to confirm the P/Invoke path actually works, not just the math — passes. Builds clean, full app runs without a controller attached (right-stick/trigger events simply never fire, same pattern as the rest of the gamepad code). **Not tested with a physical controller** — same caveat as the original controller-nav work; the stick-to-cursor feel (speed, deadzone tightness) especially needs real-hands verification. |
| 2026-08-06 | Built the named-pipe IPC transport between Service and Tray/Launcher (V2 roadmap item, tech stack table's "IPC \| Named Pipes"). `Core/Ipc/PipeFraming.cs` (length-prefixed UTF-8 message framing), `PipeMessages.cs` (`PipeRequest`/`PipeResponse` records), `CartridgeOsPipeServer.cs` (one connection = one request/response, not a long-lived duplex session — simplest correct model for occasional commands), `CartridgeOsPipeClient.cs` (`SendAsync`, returns null on any failure — Service not running, timeout, busy pipe — never throws to the caller). Scoped this deliberately to **just the transport plus one real end-to-end use**, not a full migration of scanning/controller ownership into the Service (that's a much bigger, separate task — see context.md). `CartridgeOS.Service`'s `Worker.cs` now hosts the pipe server for real, answering `Ping` and `GetGameCount` (queries the actual shared `GameDatabase`, not a stub). Tray gets a new "Service Status" context menu item that calls it and shows a balloon tip. Hit and fixed the same UI-thread-deadlock class as the artwork cache before ever shipping it: added `ConfigureAwait(false)` throughout `PipeFraming`/`CartridgeOsPipeServer`/`CartridgeOsPipeClient` proactively, since the self-check blocks synchronously on the UI thread. Added `--self-check-ipc` (spins up a real server + client in-process against a throwaway DB — genuine framing/connect/request/response round-trip, not mocked) — hit a second real bug during that: `Microsoft.Data.Sqlite` pools connections by default, so `Dispose()` doesn't release the file handle, and the self-check's temp-file cleanup failed with a sharing violation; fixed by calling `SqliteConnection.ClearAllPools()` before deleting (test-only fix, didn't change `GameDatabase`). Also added `--ipc-ping`, a genuine cross-process diagnostic (unlike the self-check, doesn't spin up its own server) — used it to start the real `CartridgeOS.Service.exe` and ping it from a separate `CartridgeOS.Launcher.exe` process, confirming actual inter-process communication, not just same-process. Self-check passes, cross-process ping against the real Service succeeded, all four projects build clean, Launcher/Tray both run without error. |
| 2026-08-06 | Built sound effects (V2 roadmap item). No real audio assets exist, so generated two placeholder tones the same way the app icon was derived earlier when there was no source asset: `Assets/Sounds/nav.wav` (60ms soft 880Hz tick) and `confirm.wav` (two-tone 660→990Hz rising chime), raw 16-bit PCM synthesized via a PowerShell script (not build-integrated — rerun manually if these need regenerating, or just replace the files once real sound design exists). `Services/SoundService.cs` plays them via `System.Media.SoundPlayer` (stdlib, no new dependency), pre-buffered via `LoadAsync()`, wrapped in try/catch so a missing/corrupt file can never break navigation. Wired `PlayNavigate()` into `MainWindow.HandleInput` (only when the selection index actually changes, not on a clamped no-op at the grid edge) and `PlayConfirm()` into `LaunchSelected`. WAV files marked `Content`/`CopyToOutputDirectory` in the csproj (not `Resource` — `SoundPlayer` needs a real file path, not a pack-embedded stream). Added `--self-check-sound` (verifies both files exist and `SoundPlayer.Load()` succeeds — can't verify anything audible headlessly, this just catches a missing/malformed file). Self-check passes, files confirmed copied to the build output, full app builds clean and runs without crashing. **Not verified that anything is actually audible** — no way to check that from this environment; listen for it yourself. |
| 2026-08-06 | Built background rescanning (V2 roadmap item). `MainViewModel` now has a `DispatcherTimer` (15-minute interval, hardcoded until there's a settings screen) that re-runs the 7 trusted launcher scanners — deliberately never the heuristic standalone scanner, since popping its confirmation dialog unprompted in the background would be bad UX. Extracted the shared scan list into `ScanTrustedLauncherSources()`, used by both the manual "Scan for Games" button (sync) and the timer tick (`RescanInBackgroundAsync`, wrapped in `Task.Run` so periodic registry/file I/O doesn't cause a UI hitch — result still merged back via the existing `ImportScannedGames` on the UI thread). `MainWindow`'s existing `Closed` handler now also calls `StopBackgroundRescanning()` alongside the existing gamepad-stop, so the timer doesn't outlive the window. Verified: builds clean, full app starts and closes without error (confirms the timer starts and stops cleanly); did not wait out a real 15-minute interval to observe an actual tick — correctness there rests on code inspection, not an observed run. Also: this build hung once under `dotnet build` while Visual Studio had the solution open — moved to background, finished on its own (exit 0) once VS presumably let go of a file lock. Not a code issue; worth remembering if a build seems to hang while VS is open. |
| 2026-08-06 | Built the standalone-executable scanner — finds games not registered with any launcher (README's "detects standalone executables" feature). `Core/Scanning/StandaloneExecutableScanner.cs` scans top-level folders under `Program Files`/`Program Files (x86)`, reuses `ExecutableHeuristics.FindLikelyGameExecutable`, and blocks known launcher/vendor-hub folder names (Steam, Common Files, Microsoft*, etc.) that would otherwise look like a "game" to the heuristic. Unlike every other scanner, this one is deliberately **not** wired into auto-add: it's pure heuristic (no manifest, no registry, guaranteed false positives — e.g. any vendor folder not in the blocklist), so results go through a new confirmation dialog (`ScanResultsWindow` + `ScanResultsViewModel`/`CandidateGameViewModel`, checkboxes default-checked, user unchecks false positives) before `ImportScannedGames` runs. Wired to a new "Find More Games" header button (`MainViewModel.FindMoreGamesCommand`), separate from "Scan for Games" (the 7 trusted launcher scanners). Added `--self-check-standalone` (real temp directory shaped like Program Files — a genuine game folder, a "Steam" folder that must be blocked, a "Common Files" folder that must be blocked, an empty folder — verifies only the real game surfaces). Builds clean, self-check passes, full app runs. This directly answers "how does it handle cracked games" from earlier: the scanner has no concept of DRM/license status, it just finds exe-shaped folders — a cracked and a legitimately-purchased DRM-free game look identical to it. |
| 2026-08-06 | Built scanners for GOG, Ubisoft Connect, EA App, Battle.net, and Riot. Confidence differs sharply from Steam/Epic here: none of the first four publish a documented manifest file, so built `Core/Scanning/InstalledProgramsScanner.cs` (walks the standard Windows uninstall registry — `DisplayName`/`Publisher`/`InstallLocation`, the one signal all traditional Windows installs share) + `Core/Scanning/PublisherGameScanner.cs` (filters those entries by publisher keyword) + `Core/Scanning/ExecutableHeuristics.cs` (guesses the main exe as the largest non-installer `.exe` in the install root, since the uninstall registry doesn't give an exe directly). One `PublisherGameScanner` instance per launcher (GOG/Ubisoft/EA/Battle.net), no per-launcher subclasses needed. Riot is more solid: `Core/Scanning/RiotManifest.cs` + `RiotScanner.cs` read Riot's own documented `RiotClientInstalls.json` (games route through the Riot Client exe itself, not a per-game binary). Consolidated the header UI from separate Scan Steam/Scan Epic buttons into one "Scan for Games" button (`MainViewModel.ScanForGamesCommand`) running all 7 sources through the existing `ImportScannedGames` dedupe path — 7 individual buttons would've been bad UX. Collapsed the growing if-chain of self-check flags in `App.xaml.cs` into a `Dictionary<string, Func<bool>>` lookup (4th+ repetition of the same pattern). Added `--self-check-riot` (sample JSON) and `--self-check-executable-heuristics` (real temp directory with fake exes of different sizes — genuinely testable end-to-end, unlike the registry-dependent parts). All 5 self-checks pass, builds clean, full app runs. **None of the 5 new scanners tested against real installs** — confidence is lower for GOG/Ubisoft/EA/Battle.net specifically since the exact registry `Publisher` string and the exe-guessing heuristic are unverified assumptions, not documented facts the way Steam/Epic/Riot's manifests are. Xbox/Microsoft Store explicitly NOT attempted — UWP/MSIX packages don't appear in the uninstall registry at all, detecting them needs the Windows package-manager APIs (`Get-AppxPackage`/`PackageManager`), a genuinely different mechanism; scoping that as its own task rather than bolting it on. |
| 2026-08-06 | Built the Epic Games Launcher scanner. `Core/Scanning/EpicManifest.cs` — parses a single `.item` manifest (JSON despite the extension, via `System.Text.Json`, stdlib) into a `Game`, pulled out separately so it's testable without a real Epic install. `Core/Scanning/EpicScanner.cs` — reads every `*.item` file under `%ProgramData%\Epic\EpicGamesLauncher\Data\Manifests`. Unlike Steam, Epic's manifest gives a direct install path + launch exe, so games launch as a normal exe path (no protocol-URI trick needed) — but that also means no built-in artwork source the way Steam's `librarycache` gave us; `ArtworkPath` is left null for now (falls back to the placeholder icon), revisit if Epic's local thumbnail cache location turns out to be worth parsing. Refactored `MainViewModel`'s Steam-only merge/dedupe logic into a shared `ImportScannedGames(IEnumerable<Game>)` used by both `ScanSteamCommand` and the new `ScanEpicCommand` (second scanner made the duplication worth removing). Added a "Scan Epic" header button next to Scan Steam. No Epic install on this dev machine, so added `--self-check-epic` (`Services/EpicManifestSelfCheck.cs`) testing `EpicManifest.Parse` against realistic sample JSON plus malformed/missing-field cases — passes (exit 0). Builds clean, full app still runs. **Not tested against a real Epic install.** |
| 2026-08-06 | Built the Steam scanner (first V2 auto-detection source). `Core/Scanning/SteamVdf.cs` — minimal regex-based reader for Valve's KeyValue (VDF) text format, just the fields we need (library `path`, `appid`, `name`), not a general parser. `Core/Scanning/SteamScanner.cs` — reads Steam's install path from `HKCU\Software\Valve\Steam\SteamPath` (added `Microsoft.Win32.Registry` package to Core), walks `libraryfolders.vdf` for every library, then every `appmanifest_*.acf` in each; games launch via `steam://rungameid/{appid}` (no exe-path resolution needed — Steam's protocol handler deals with launchers/DRM/wrappers itself) with artwork sourced from Steam's own `librarycache` if present. Wired into `MainViewModel.ScanSteamCommand` (dedupes against existing `ExecutablePath`s) and a "Scan Steam" header button next to Add Game. No Steam install exists on this dev machine, so added `Services/SteamScannerSelfCheck.cs` (behind `--self-check-steam`) that feeds realistic sample VDF/ACF text directly into `SteamVdf`'s pure parsing functions — passes (exit 0). Bumped `CartridgeOS.Core` and `CartridgeOS.Service` from `net9.0` to `net9.0-windows` (registry access produced `CA1416` platform-compat warnings on the plain TFM; the whole app is Windows-only anyway, so this just makes that explicit rather than suppressing the warning). Builds clean across all 4 projects, both self-checks pass, full app still runs. **Not tested against a real Steam install** — only the VDF-parsing logic is verified; the registry lookup / file-walking / artwork-path guessing are unverified against real Steam data. |
| 2026-08-06 | Built the controller input wrapper: `Input/XInput.cs` (raw P/Invoke of `XInputGetState` from `xinput1_4.dll` — no SharpDX/Vortice dependency, XInput is a 1-function native API), `Input/GamepadWatcher.cs` (background poll loop at ~30Hz, edge-detects A/B/X/Y/Start/Back, folds left-stick-with-deadzone into the same dpad-direction bits, held-direction repeat with 400ms initial delay / 130ms repeat). Wired into `MainWindow.xaml.cs`: dpad/stick moves `SelectedGame` through the tile grid (column count derived from `ListBox.ActualWidth` at nav time — no formal grid-layout abstraction, kept in code-behind since it's the only consumer), A launches the selected game's exe if set. Verified builds clean and runs without crashing with no controller attached (confirms the not-connected path doesn't throw); have not tested with a physical controller — do that before calling this feature done. |
| 2026-08-07 | Multi-drive fix for `StandaloneExecutableScanner`: `DefaultScanRoots()` no longer only checks `Program Files`/`Program Files (x86)` on the system drive — now also enumerates every other fixed drive (`DriveInfo.GetDrives()`, `DriveType.Fixed`) and adds that drive's `Program Files` and `Games` folders. Steam/Epic/Riot scanners already read real install paths from registry/manifests regardless of drive, so they needed no change — confirmed by reading `SteamScanner`/`EpicManifest`/`RiotManifest` rather than guessing. Builds clean. |
| 2026-08-07 | Fixed Steam artwork not loading: `SteamScanner.FindArtwork` was only checking the old flat `{appid}_library_600x900.jpg` filename under `appcache/librarycache`; Steam clients since ~2022 nest images per-appid (`librarycache/{appid}/library_600x900.jpg`). Now checks the nested path first, falls back to the flat one. |
| 2026-08-07 | Built internet artwork fallback for games with no local art source (Epic/Riot/GOG/Ubisoft/EA/Battle.net/standalone/Xbox — everything except Steam-with-local-cache and manually-added games with user-picked art). New `Core/Scanning/ArtworkFetcher.cs`: Steam games (appid parsed out of the `steam://rungameid/{appid}` exe path) hit Valve's public CDN directly, no key needed; everything else is looked up by title against TheGamesDB (`Games/ByGameName` → first match's id → `Games/Images?filter[type]=boxart` → front boxart + `base_url.large`), gated behind the `CARTRIDGEOS_THEGAMESDB_KEY` env var (user has a free key, set as a permanent User env var — **note: only visible to processes started after it was set, an already-open terminal/IDE won't see it**). Downloads once, caches to `%LocalAppData%\CartridgeOS\ArtworkCache\downloaded\{title}_{id}.jpg`, never re-fetched once cached. `GameDatabase.UpdateArtworkPath` + `GameTileViewModel.SetArtworkPath` persist and hot-swap the fetched art into the already-showing tile. Wired into both `MainViewModel.ImportScannedGames` (real scan results) and the constructor's initial `_db.GetAllGames()` load (so the seed/demo games get artwork too, since the user wanted to test without real games installed) — fire-and-forget per game, never blocks the UI. Added file logging (`%LocalAppData%\CartridgeOS\artwork.log`, plain append, no rotation) after the user pointed out there was no way to tell if a fetch was happening or why it failed — logs cache hits, source URL chosen, HTTP status/body on every non-2xx TheGamesDB response, and download success/failure per game. Diagnosed the actual "still no artwork" report by testing the API key directly via PowerShell (search + images + CDN download all confirmed working end-to-end for "Half-Life 2") — concluded the real cause was environment-variable propagation (stale terminal/IDE process), not the code or the key. Builds clean. **Known weak spot, not fixed**: TheGamesDB title matching takes the first search result with no scoring/disambiguation — a title with multiple entries (different regions/platforms/remasters) can resolve to the wrong one. |
| 2026-08-07 | Built a collapsible settings sidebar — first piece of a real settings screen, starting with a "Background" option per user request. New `Core/Models/AppSettings.cs` (`WallpaperMode` enum: `SelectedGameArtwork`/`CustomImage`, `CustomWallpaperPath`) + `Core/Data/SettingsStore.cs` (plain JSON file at `%LocalAppData%\CartridgeOS\settings.json` — too small to justify a SQLite table). `MainViewModel` gained `IsSettingsOpen`/`ToggleSettingsCommand` (gear-icon button in the header toggles a right-side panel, plain `Visibility` swap, no slide animation), `WallpaperMode` (two-way, persists on change), `ChooseWallpaperCommand` (file picker, switches mode to `CustomImage` and decodes+caches the chosen image via the existing `ArtworkCache.LoadAsync` at 1920px width — reused rather than reinventing decode/cache logic). `MainWindow.xaml` now has two background `Image`s (`BackgroundArt` for game artwork, `CustomBackgroundArt` for the custom wallpaper) toggled by `IsUsingGameArtworkBackground` via a new `InverseBooleanToVisibilityConverter`; the existing per-selection fade-in animation in `MainWindow.xaml.cs` now dispatches to whichever image actually changed. New `Converters/EnumToBooleanConverter.cs` for binding the two mode `RadioButton`s to the single `WallpaperMode` enum property (standard WPF pattern, not a new dependency). Builds clean. **Not yet visually verified** — no screenshot capability here, check the panel opens/closes and both wallpaper modes render correctly by hand. |
| 2026-08-07 | User wanted the background art filling the full screen again (letterboxing from the previous `Uniform` change left empty space at the sides). Switched `BackgroundArt` back to `Stretch="UniformToFill"`, kept `BlurEffect Radius="20"` from the prior fix — the blur-strength complaint and the aspect-ratio complaint were separate issues, no need to revert both together. |
| 2026-08-07 | User reported the background art was too blurry and the aspect ratio looked off. Root cause: box art is portrait (600x900), and `Stretch="UniformToFill"` was force-scaling it up to cover the landscape screen — that upscale zoom was compounding with the actual `BlurEffect` to look like mush. Changed to `Stretch="Uniform"` (shows the full image at its real proportions, letterboxed into the dark background instead of blown up) and cut `BlurEffect Radius` from 60 to 20. |
| 2026-08-07 | User reported the background art was barely visible under the overlay. Bumped `BackgroundArt` `Opacity` from 0.45 to 0.85 and roughly halved the dark gradient overlay's alpha (`#B0/#E0` → `#40/#90` on `#0E1420`) in `MainWindow.xaml`, kept `MainWindow.xaml.cs`'s `BackgroundArtOpacity` fade target in sync. Builds clean. |
| 2026-08-07 | Built the PS5-menu-style blurred background: `MainWindow.xaml` has a new full-window `BackgroundArt` `Image` (bound to `SelectedGame.Artwork`, `BlurEffect Radius=60`, `Opacity=0.45`) plus a dark gradient overlay, both behind the existing header/grid content (`Grid.RowSpan="3"`, `IsHitTestVisible="False"`). `MainWindow.xaml.cs` fades `BackgroundArt` back in (0 → 0.45 opacity, 250ms `DoubleAnimation`) whenever `MainViewModel.SelectedGame` changes via `PropertyChanged` — covers both mouse-click and gamepad/keyboard nav since both set the same bound property. Games with no artwork yet just show no image there (falls back to the plain dark background, no extra null-handling needed). Builds clean. **Skipped**: true two-image crossfade (old art fading out while new fades in) — a single fade-in was judged close enough to the PS5 feel; revisit if the swap still looks like a hard cut in practice. |

| 2026-08-07 | Started Phase 3 with the in-game overlay (user's pick of the five Phase 3 items to start with). `Input/GlobalHotkey.cs` — raw `RegisterHotKey`/`WM_HOTKEY` P/Invoke (same pattern as `XInput.cs`/`MouseEmulator.cs`), fires `Ctrl+Shift+O` even when the launcher window has no focus, which is the whole point since a separately-launched game owns focus. `OverlayWindow.xaml`/`.xaml.cs` + `ViewModels/OverlayViewModel.cs` — small borderless always-on-top panel docked bottom-right (`SystemParameters.WorkArea`-based positioning), shows the running game's title, "Return to Cartridge OS", and "Quit Game". `MainWindow.xaml.cs`'s `LaunchSelected` (converted from `static` to an instance method to get at window/overlay state) now tracks the launched `Process`, wires `Exited` (dispatched back to the UI thread), and — important, this was a real latent bug the overlay exposed — minimizes the main window on launch: it's `Topmost="True"` (kiosk-style, set back in V1) and maximized, so without minimizing it would sit visually on top of whatever game just launched, hotkey or not. `OnGameExited` restores it. **Known, deliberate limitation**: `Process.Start(UseShellExecute: true)` returns `null` for shell-protocol launches — `steam://rungameid/{id}` (all Steam games) and `shell:appsFolder\...` (Xbox/Store games) — so there's no process to track and the overlay/hotkey silently does nothing for those; only directly-launched exes (manually added, standalone-scanner finds, Epic, GOG/Ubisoft/EA/Battle.net, Riot's client exe) get overlay support. Also **not implemented**: gamepad button for toggling the overlay (keyboard-only hotkey for now), and DirectX-exclusive-fullscreen games likely won't show the overlay on top even though it's `Topmost` (same class of limitation every non-driver-level overlay has — Discord/Steam solve this with actual graphics-API hooking, out of scope here). Builds clean. **Not hands-on tested** — no way to launch and verify a real running game from this environment; verify the hotkey toggles, Return/Quit both work, and the minimize-on-launch doesn't feel jarring, by hand. |
| 2026-08-07 | Added a controller toggle for the overlay per user request: `GamepadButton.Start` (Xbox "Menu"/PlayStation "Options", already edge-detected by `GamepadWatcher` but previously unwired) calls the same `ToggleOverlay()` as the keyboard hotkey, in `MainWindow.xaml.cs`'s `HandleInput`. Notably this works without window focus for free — `GamepadWatcher`'s poll loop runs on a background thread independent of focus, unlike keyboard input which needed the `RegisterHotKey` P/Invoke specifically to bypass the focus requirement. Updated the overlay's hint text to mention both toggle methods. Builds clean, not hands-on tested with a physical controller (same standing caveat as the rest of the gamepad code). |

| 2026-08-07 | Evaluated Suspend/Resume (next Phase 3 item) and decided **not to build it**, per user instruction after explaining the tradeoff — recording the full reasoning here rather than just the checklist line. **What it would actually be**: unlike a console (which snapshots a game's full RAM/GPU state to disk so it can fully close and resume instantly later), Windows has no OS-level equivalent for suspending an arbitrary desktop process. The only thing technically achievable is *freezing the process's threads* via the undocumented `NtSuspendProcess`/`NtResumeProcess` NTAPI functions — the same mechanism Process Explorer's "Suspend" button uses. That stops CPU usage while frozen but does **not** release GPU/VRAM, does **not** serialize any state, and the process resumes exactly where its threads left off (not equivalent to a real pause of game logic/timers either — a frozen game with a live network socket just stops responding rather than "pausing" gracefully). **Why it's risky, not just unpolished**: anti-cheat software (EAC, BattlEye, VAC) actively watches for externally frozen threads as a tampering signal — suspending a process running one of these can trigger a ban, and this covers most competitive/online games, not a rare edge case. It's also built on an undocumented API with no Microsoft stability guarantee across Windows versions. The realistic benefit (freeing CPU momentarily for single-player, non-anti-cheat games) is a narrower slice of the actual game library than "Suspend/Resume" as a checklist item implies. Given this is meaningfully riskier than anything else shipped so far (nothing else here can get a user's game account banned), it needs a much stronger case before it's worth building — revisit only if there's a specific, safer use case (e.g. scoped to a user-maintained allowlist of known-safe single-player titles) rather than a blanket feature. |

| 2026-08-07 | Built Discord Rich Presence (next Phase 3 item, user approved after risk/importance rundown — low risk compared to Suspend/Resume, no anti-cheat concern). `Services/DiscordRichPresence.cs` — raw implementation of Discord's local IPC protocol over `NamedPipeClientStream` (`\\.\pipe\discord-ipc-{0..9}`, opcode+length-prefixed JSON frames, handshake then `SET_ACTIVITY` commands), no `discord-rpc` NuGet dependency needed for this scope. Every public method swallows its own `IOException`/`TimeoutException` — Discord not running, or quitting mid-session, must never affect game launching (same defensive pattern as `ArtworkCache`/`SoundService`). Wired into `MainWindow.xaml.cs`: connects on `Loaded` (fire-and-forget, no-op if Discord isn't running), sets activity (`game.Title` + start timestamp) right after every launch — **deliberately before the `Process is null` check**, so it also covers Steam/Xbox shell-launches that the overlay/process-tracking can't reach, at the cost of not being able to auto-clear those on exit — and clears it in `OnGameExited` (only reachable for directly-tracked processes). **Client ID is a placeholder** (`DiscordRichPresence.cs`'s `ClientId` constant, `ponytail`-marked) — user opted to build now and supply the real one later rather than block on registering a Discord application first; nothing will actually show on Discord until that ID is real. Not a secret, safe to hardcode once provided. Builds clean, not hands-on tested (needs both a real Client ID and Discord actually running). |

| 2026-08-07 | Evaluated Cloud Sync (next Phase 3 item) and skipped it, per user instruction after explaining the tradeoff. Unlike Suspend/Resume (skipped for safety) or Discord RPC (built, cosmetic), this one directly **contradicts a stated design goal**: the README's "fully offline, no accounts" pitch. Syncing a library/playtime/settings across machines needs either a real backend (accounts, auth, a database, ongoing hosting — a different category of commitment than anything built so far) or piggybacking on a third-party account (Google Drive/Dropbox API), which reintroduces the "accounts" the app explicitly avoids. Also the most expensive remaining Phase 3 item by far. Left as an open checklist item rather than removed from the roadmap (user didn't ask for the README/roadmap itself to change, just to skip building it now) — revisit only if the offline-first positioning changes. |

| 2026-08-07 | Evaluated Plugin Support (last Phase 3 item) and skipped it, per user instruction after explaining the tradeoff — this closes out the Phase 3 evaluation pass (overlay built, suspend/resume skipped for anti-cheat risk, Discord RPC built pending a real Client ID, cloud sync skipped for contradicting the offline-first pitch, plugins skipped here). Unlike a feature, this is committing to a public extension API (`IScannerPlugin`-style interfaces, an `AssemblyLoadContext`-based loader, a versioning/compatibility story) that has to be supported indefinitely once anything depends on it — and there are currently zero plugins or external contributors to validate the design against (solo closed-source project, per context.md's working agreements). Designing that API now would be a guess likely to be wrong once — if ever — a real use case shows up. Revisit only when there's a concrete "I want to extend X without recompiling" need, and scope it narrowly to that need rather than a general plugin system. |

| 2026-08-07 | User supplied the real Discord Application ID (`1535179486117502976`). Replaced the placeholder `ClientId` in `Services/DiscordRichPresence.cs`, cleaned up the now-stale placeholder comment. Discord Rich Presence is functionally complete — builds clean, but still not hands-on verified against a real running Discord client; launch a game and check the Discord profile/friends list shows "Playing {title}". |

| 2026-08-07 | Added SteamGridDB as the primary non-Steam artwork source (user got a key, wants it preferred over TheGamesDB since that one's capped at 1000 requests/month) plus proper HTTP 429 handling for both. `Core/Scanning/ArtworkFetcher.cs`: non-Steam lookup order is now SteamGridDB (`CARTRIDGEOS_STEAMGRIDDB_KEY`) → TheGamesDB (`CARTRIDGEOS_THEGAMESDB_KEY`) as fallback, tried in that order whenever the first returns null for any reason (not configured, no match, error, or rate-limited). New private `RateLimiter` nested class tracks a per-source cooldown (`Dictionary<string, DateTime>` + a `Lock`, shared static state across every concurrent fetch in the process) — necessary because games are fetched fire-and-forget in a loop after a scan; without a shared cooldown, a burst of many new games would fire many concurrent requests at a source that just 429'd. `HandleTooManyRequests` reads the standard `Retry-After` header (seconds-delta or HTTP-date form, both handled) and sets that source's cooldown accordingly, defaulting to 60s if the header's absent; while a source is cooling down, `FindSteamGridDbUrlAsync`/`FindTheGamesDbUrlAsync` skip calling it entirely rather than queuing/retrying inline. Verified the new SteamGridDB key directly via a manual `Invoke-WebRequest` against its search endpoint (200, real results for "Half-Life 2") before wiring it in. Set `CARTRIDGEOS_STEAMGRIDDB_KEY` as a permanent User env var (user pasted the key, same flow as TheGamesDB's key). Builds clean, not yet exercised end-to-end through the actual app (only the raw API call was manually verified). |

| 2026-08-07 | Switched both artwork API keys (SteamGridDB, TheGamesDB) from environment variables to hardcoded constants in `Core/Scanning/ArtworkFetcher.cs`, after the user asked why a new user would ever need to set env vars themselves. Correct call: env vars protect a secret from being committed to a *public* repo / from leaking out of a *server* process — neither applies here. This is closed-source and the key ships inside the compiled DLL of every install either way (trivially extractable with a decompiler regardless of literal-vs-env-var), so the env var was providing zero real protection while adding a real cost: a new install would've silently gotten placeholder tiles forever with no setup step to tell them why. Same reasoning already applied to the Discord Client ID (`DiscordRichPresence.cs`) — now consistent across all three. The `CARTRIDGEOS_STEAMGRIDDB_KEY`/`CARTRIDGEOS_THEGAMESDB_KEY` user env vars set earlier today are now dead/unused on this dev machine (harmless to leave). Builds clean. |

| 2026-08-07 | Restructured the settings sidebar into a proper Steam/NVIDIA-app-style nested layout, per user request, ahead of adding more setting categories later. `MainWindow.xaml`: replaced the flat `StackPanel` (just a "Background" section) with a native `TabControl` restyled via `SettingsTabControlStyle` (custom template: `DockPanel` with a fixed-width `TabPanel` docked left as the category rail + `ContentPresenter` for the selected category's content docked right — no new dependency, `TabControl`/`TabPanel` are stdlib WPF) and `SettingsTabItemStyle` (category row: highlighted background + bold text when selected, hover highlight otherwise). The existing wallpaper radio buttons/browse button now live inside a `TabItem Header="Wallpaper"` — same controls, just nested under an explicit category instead of a bare heading. New `SettingsSectionHeadingStyle` factors out the "Background" sub-heading styling so future categories reuse it instead of re-specifying `FontSize`/`FontWeight`/margin inline. Widened the sidebar `Border` from 320 to 460 to fit both the category rail and detail content comfortably. Adding a new category going forward is just another `<TabItem Header="...">` block using the same three styles — documented with a comment at the `TabControl` site. Builds clean. Not yet visually verified — no screenshot capability here, check the category rail renders correctly and clicking "Wallpaper" shows its settings by hand. |

| 2026-08-07 | Built Steam-style single-instance launcher + tray behavior with disposable-UI memory management, per detailed user spec. **Merged `CartridgeOS.Tray` into `CartridgeOS.Launcher`** (user confirmed, project deleted, `.sln` updated) — `CartridgeOS.Launcher.exe` is now the single process users run; it owns the tray icon directly instead of Tray spawning Launcher as a child process. **`App.xaml.cs` is now the "core"**: `Mutex(@"Global\CartridgeOS.SingleInstance")` for single-instance detection; a second launch signals the running instance over a dedicated named pipe (`CartridgeOS.Launcher.SingleInstance` — parameterized `CartridgeOsPipeServer`/`CartridgeOsPipeClient`'s previously-hardcoded pipe name so this coexists with the unrelated Service IPC pipe) then exits immediately with no window/tray created. `ShutdownMode="OnExplicitShutdown"` (`App.xaml`) means closing the launcher window never quits the app on its own. **Core-owned services that must outlive the launcher window being closed** — moved from `MainWindow.xaml.cs` to `App.xaml.cs`: `GamepadWatcher`, `MouseEmulator`, `GlobalHotkey` (now registered against a new invisible always-alive `_coreWindow`, `Width=0 Height=0 Visibility=Hidden`, `WindowInteropHelper.EnsureHandle()` since it's never shown — `GlobalHotkey.cs` changed from `.Handle` to `.EnsureHandle()` to support this), `DiscordRichPresence`, running-game process tracking, and the whole overlay flow (`ToggleOverlay`/`ReturnToLauncher`/`QuitRunningGame`) — none of that can safely live on a window that might not exist. Gamepad button routing: App subscribes once; `Start` always toggles the overlay (works with no launcher window open), everything else forwards to `_launcherWindow?.HandleGamepadButton(button)` if a window exists. **`MainWindow.xaml.cs` is now genuinely disposable**: no long-lived service references, `App.ShowLauncher()` creates it (passing the last-selected game id to restore) and `App.OnLauncherClosed()` (wired to the window's `Closed` event) captures that one bit of state, drops the reference, and does one deliberate `GC.Collect()` + `GC.WaitForPendingFinalizers()` pass so freed artwork bitmaps/decoded images are reclaimed promptly rather than waiting for an incidental GC — the "destroy and recreate beats hidden-resident-window" approach works here specifically because WPF `BitmapImage`s aren't `IDisposable`; dropping every reference to the closed window/viewmodel/tile graph and forcing a collection is the correct way to actually free that memory, not a manual per-object dispose loop. `LaunchGame` (moved from `MainWindow.LaunchSelected`) now lives on `App`, called via `((App)Application.Current).LaunchGame(vm, game)`. **User-confirmed behavior choices** (asked via 3 questions before implementing, given the size of this change): minimize (not fully destroy) the launcher window on game launch — cheaper, no recreate-flicker, still satisfies "tray+core survive independently"; auto-reopen the launcher when the game exits (Steam-like); remove the Tray project rather than leave it unused. Tray context menu simplified to just **Open Cartridge OS** / **Exit Cartridge OS** (dropped the old "Service Status" item — Service is separate, unrelated infrastructure, mixing its status into the merged app's menu didn't make sense here). `ExitApplication()` tears down every core resource in order: cancels the single-instance pipe server, stops the gamepad poll loop, disposes the hotkey/Discord connection, closes the overlay and launcher window, disposes the tray icon, closes the core window, releases+disposes the mutex, then `Shutdown()`. **Hands-on verified** (not just build-clean) via PowerShell + raw Win32 calls (`EnumWindows`/`GetWindowThreadProcessId` to find the real window handle — `Get-Process.MainWindowHandle` came back 0 for this borderless/topmost window, so that couldn't be used directly; `PostMessage(hwnd, WM_CLOSE, ...)` to close it programmatically without needing real UI focus, working around this environment's [documented UI-automation limitation](#environment-limitation-no-real-ui-automation-here)): (1) started the app, confirmed one process; (2) launched the exe a second time, confirmed it exited immediately (code 0) with no second process created; (3) sent `WM_CLOSE` to the real launcher window handle, confirmed the window closed but the process (same PID) kept running; (4) launched the exe again, confirmed the *same* process's window reopened (title back to "Cartridge OS") rather than a new process starting. All four confirm the core single-instance/disposable-UI/tray-survival requirements actually work, not just compile. **Not verified by hand**: the tray icon's visual appearance/context-menu clicks (no way to click a real system tray icon from this environment), the in-game overlay's Start-button/hotkey toggle while a real game process is running, and Discord presence during this flow — same standing "verify by hand" caveats as the features these build on. Also updated `README.md` (startup instructions, tray menu, keyboard/controller tables, running-and-testing section) and `production-readiness.md` (stale Tray references) to match. Builds clean, 0 warnings/errors, across all 3 remaining projects (Core/Launcher/Service). |

| 2026-08-08 | Added PlayStation controller support (checklist item: "Add D input support for playstation controllers" — XInput never sees DualShock/DualSense since Windows doesn't map them to it). `Input/RawGameControllerSource.cs`: falls back to `Windows.Gaming.Input.RawGameController` (DirectInput-backed) whenever `XInputGetState` fails, shaping its generic HID reading (buttons/switches/axes) into the existing `XInputGamepad` struct so `GamepadWatcher`'s poll/repeat/deadzone logic is reused unchanged rather than duplicated. Button/axis order follows the common HID convention DualShock/DualSense report (Square/Cross/Circle/Triangle, LX/LY/RX/RY/LT/RT) — `ponytail`-marked as not API-guaranteed, recalibrate if a specific pad maps wrong. Controller brand is detected via `RawGameController.HardwareVendorId` (Sony = `0x054C`). Bumped `CartridgeOS.Launcher`'s TFM to `net10.0-windows10.0.19041.0` (from plain `net10.0-windows`) to get the WinRT projection — no new NuGet package. Along the way, normalized the input layer per a follow-up request: replaced the physical-button `GamepadButton`/`ButtonPressed` event the UI used to consume with a `GamepadAction` vocabulary (`NavigateUp/Down/Left/Right`, `Confirm`, `Back`, `Secondary`, `Menu`) translated inside `GamepadWatcher` — `MainWindow.xaml.cs`/`App.xaml.cs` no longer know which physical button did what. Added `ControllerKind` (Xbox/PlayStation/Generic) + `ControllerGlyphs` (per-brand button labels) and wired a real consumer: the in-game overlay's toggle hint now shows "Start" or "Options" depending on what's actually plugged in (`OverlayViewModel.MenuButtonLabel`, updated live via `GamepadWatcher.ControllerChanged`). `X` and the physical view/select "Back" button are still unmapped — never wired to any launcher behavior, left out rather than inventing unused actions. Builds clean (0 warnings), `--self-check-mouse-emulation` and `--self-check-sound` still pass (untouched math/logic, just re-verified after the TFM bump). **Not hands-on tested with a physical PlayStation controller** — no DualShock/DualSense available in this environment; the button/axis mapping is the main thing that needs real-hardware verification. |

| 2026-08-08 | Fixed the "Cannot find the .\data\ directory!" launch bug: `App.xaml.cs`'s `LaunchGame` builds the `ProcessStartInfo` without setting `WorkingDirectory`, so it defaulted to the .NET process's own folder — any manually-added game that resolves assets via a relative path (`.\data\`) looked in the launcher's `bin\Release\...` instead of its own install folder. Fixed by setting `WorkingDirectory = Path.GetDirectoryName(game.ExecutablePath)`. Builds clean. |
| 2026-08-08 | Fixed Wallpaper Engine (and any game) getting added twice by a scan: `MainViewModel.ImportScannedGames` built its "already exists" `HashSet<string>` once before the loop and never grew it as games were imported, so two entries sharing the same `ExecutablePath` *within the same scan batch* both passed the check — e.g. `SteamScanner` reading the same `appmanifest_*.acf` twice because `libraryfolders.vdf` listed a library path twice with a trailing-slash/casing difference. Switched the check to `existingExePaths.Add(...)`'s return value so the set grows as each game is added, catching intra-batch duplicates too. Forward-fix only — doesn't retroactively remove a duplicate already sitting in `games.db`, which is why the Remove Game feature (next entry) shipped in the same session. Builds clean. |
| 2026-08-08 | Added a Remove Game feature, requested directly off the Wallpaper Engine duplicate report (no way to clean up an existing duplicate without it). `GameDatabase.DeleteGame(id)` — plain `DELETE FROM Games WHERE Id = $id`. `MainViewModel.RemoveGameCommand` confirms via `MessageBox` (Yes/No, warning icon — deletion is exactly the kind of data-loss-risking action that doesn't get the lazy no-confirmation treatment) before calling it, then removes the tile from `Games` and re-selects the next game / rebuilds the Recent row. New "Remove Game" header button next to "+ Add Game" in `MainWindow.xaml`, calls the command on whatever's currently selected. **Skipped**: disabling the button when nothing's selected (would need a new null-to-bool converter for one binding; `RemoveGame()` already no-ops safely if `SelectedGame` is null, so clicking with nothing selected just does nothing rather than erroring) — add a converter if the no-op-button UX turns out to bother anyone in practice. Also skipped a gamepad/keyboard binding for removal (X is currently unmapped in the `GamepadAction` vocabulary) — deletion via a physical button with no on-screen confirmation prompt felt riskier than worth adding without being asked. Builds clean. |

| 2026-08-08 | Built the header status bar — controller/device battery, network connectivity, search, and clock — matching a reference design the user supplied. **Battery**: `Input/XInput.cs` gained `GetBatteryPercent` (`XInputGetBatteryInformation`, bucketed since XInput only reports 4 coarse levels not a real percentage), `Input/RawGameControllerSource.cs` gained `GetBatteryPercent` (`RawGameController.TryGetBatteryReport()`, real percentage from remaining/full milliwatt-hours), `GamepadWatcher` polls whichever is active at most every 10s (not every 33ms poll tick — a real syscall each time) and exposes `ControllerBatteryPercent`/`ControllerBatteryChanged`. New `Services/DeviceBattery.cs` (raw `GetSystemPowerStatus` P/Invoke, same pattern as the rest of `Input/`) covers this machine's own battery as the fallback when no controller reports one, and returns null on a desktop with no battery at all. `App.xaml.cs` forwards `ControllerBatteryChanged` to whichever `MainWindow` currently exists (same pattern as the existing `ControllerChanged`→overlay-label forwarding) and pushes the current value on window creation; caught and fixed a real latent bug while in there — `OnControllerChanged` was touching a bound viewmodel property from `GamepadWatcher`'s background poll thread with no `Dispatcher.BeginInvoke`, same class of bug `OnGamepadAction` already guards against. `MainViewModel.RefreshBatteryDisplay()` combines controller-or-device into one `BatteryGlyph`/`BatteryLabel`/`HasBatteryInfo`. **Connectivity**: `NetworkChange.NetworkAvailabilityChanged` (stdlib) drives `IsOnline`/`ConnectivityGlyph`/`ConnectivityLabel`. **Search**: `MainViewModel.GamesView` (`CollectionViewSource.GetDefaultView(Games)`, `Filter` on `SearchText`) is now what the grid binds to instead of `Games` directly; `MainWindow.xaml.cs`'s `HandleGamepadAction` nav was updated to walk `GamesView` (a live snapshot via `.Cast<GameTileViewModel>().ToList()`) instead of the unfiltered `Games`, so gamepad/keyboard nav stays consistent with whatever the filter is actually showing on screen — this was the one non-trivial interaction the search feature touched. Search toggle (`IsSearchOpen`) shows/hides an inline `TextBox` inside the search pill, clearing itself on close so the grid doesn't stay stuck filtered. **Clock**: a 1s `DispatcherTimer` (`_statusTimer`) formats `CurrentTimeText`/`CurrentDateText` and piggybacks a device-battery refresh on the same tick; stopped via new `MainViewModel.StopStatusUpdates()`, called from `MainWindow`'s `Closed` handler alongside the existing `StopBackgroundRescanning()` — same disposable-window lifecycle every other timer in this app already follows. New `StatusPillStyle` (`MainWindow.xaml`) — rounded dark pill, `SurfaceRaisedBrush`/`BorderBrush` — matches the app's existing brand palette rather than copying the reference image's colors directly, per the user's "same theme as ours" instruction. Builds clean (0 warnings), both self-checks still pass, app starts and stays running (process-alive check, no crash) — **not visually verified against the reference image** (no screenshot capability here); check pill spacing/alignment and that the search box doesn't clip at 140px width by hand. |

| 2026-08-08 | Added a per-game context menu (Change Wallpaper / Delete Game), reachable by right-clicking a tile or pressing gamepad Menu/Start/Options (keyboard: the Apps/context-menu key) while no game is running. `MainViewModel.ChangeArtworkCommand` opens a file picker and replaces that specific game's `ArtworkPath` (`GameDatabase.UpdateArtworkPath` + `GameTileViewModel.SetArtworkPath`, same plumbing `ArtworkFetcher` already uses) — distinct from the existing Settings-sidebar "Choose Wallpaper" which sets the app-wide custom background, not a per-game image. "Delete Game" reuses the existing `RemoveGameCommand`. `MainWindow.xaml`: a shared `GameTileContextMenu` resource attached to both `GameTileStyle` and `RecentTileStyle` via a `ContextMenu` `Setter`; its two `MenuItem`s use `Click` handlers in code-behind (`ChangeWallpaper_Click`/`DeleteGame_Click`, reading `MainWindow`'s own `DataContext` directly) rather than XAML `Binding`s through the popup, sidestepping WPF `ContextMenu`'s well-known data-context/visual-tree quirks entirely instead of fighting them. Right-click doesn't move `ListBox` selection on its own, so a new `PreviewMouseRightButtonDown` `EventSetter` (`GameTile_PreviewMouseRightButtonDown`) selects the clicked tile first, so both the mouse and gamepad paths always act on the right game. Gamepad routing: `GamepadAction.Menu` used to unconditionally toggle the in-game overlay (a no-op whenever nothing was running); `App.OnGamepadAction` now only does that when `_runningGameProcess is not null`, otherwise forwards `Menu` to the launcher window, which opens the selected tile's context menu (`MainWindow.OpenGameContextMenu`, via `GameGrid.ItemContainerGenerator.ContainerFromItem`). Builds clean, app starts and stays running (process-alive check). **Not hands-on verified** — no way to right-click or press a physical gamepad button from this environment; check that both the context menu opens correctly and that overlay-toggle-while-a-game-is-running still works unchanged. |

| 2026-08-08 | Added an artwork crop/reposition step to "Change Wallpaper", so a picked image with a different aspect ratio than the tile doesn't get silently center-cropped by WPF's `Stretch="UniformToFill"` — the user can now choose which part of the image survives the crop. New `ArtworkCropWindow.xaml`/`.xaml.cs` — a fixed 300x450 (2:3, matching the box-art convention already used elsewhere: Steam's `library_600x900.jpg`, SteamGridDB/TheGamesDB boxart) viewport `Border` with `ClipToBounds`, the source image inside on a `ScaleTransform`+`TranslateTransform` the user drags to pan; a `Slider` zooms, clamped so the image can never shrink below fully covering the viewport (standard "cover" pan/zoom — no empty gaps possible). "Use This Crop" computes the source-pixel crop rectangle from the current scale/translate, crops via `CroppedBitmap`, and saves a new PNG to `%LocalAppData%\CartridgeOS\ArtworkCache\custom\{guid}.png`. `MainViewModel.ChangeArtwork()` only opens this dialog when the picked image's aspect ratio is more than 5% off the tile's ~2:3 (`MatchesTileAspect`, checked cheaply via `BitmapFrame.Create(..., BitmapCreateOptions.DelayCreation, ...)` — just reads header dimensions, no full decode) — an image that already fits closely skips the extra step and uses the original file directly. Builds clean, app starts and stays running. **Not hands-on verified** — no way to drag a mouse or preview rendering from this environment; check that dragging/zooming feels right and the final crop actually matches what's shown in the preview viewport. |

| 2026-08-08 | Added a single-level "Revert to Previous Artwork" to the per-game context menu, so changing a game's artwork isn't a one-way door. `GameTileViewModel.SetArtworkPath` now remembers the game's prior `ArtworkPath` before overwriting it; `TryRevertArtwork(out restoredPath)` restores it and forgets the memory (calling it twice in a row does nothing the second time — this is undo-last-change, not a history stack). `GameDatabase.UpdateArtworkPath` now accepts `string?` (writes `DBNull` on null) so reverting a game that had no artwork before its most recent change correctly clears it back to the placeholder, not just in the DB but on the tile too (`TryRevertArtwork` explicitly nulls `Artwork` in that case, since `LoadArtworkAsync` intentionally leaves it alone on a null path). `MainViewModel.RevertArtworkCommand` wires it through the same DB-update pattern as `ChangeArtwork`. **Scope note, not a bug**: this is in-memory per `GameTileViewModel` instance — since the launcher window (and everything under it) is disposable and recreated on close/reopen, the "previous" memory does not survive closing the launcher; reverting only undoes a change made in the current session. Builds clean, app starts and stays running. **Not hands-on verified.** |

| 2026-08-08 | Restyled the per-game context menu (Change Wallpaper / Revert to Previous Artwork / Delete Game) to match the app's own theme — the native Win32 `ContextMenu`/`MenuItem` chrome (gray, icon/check gutter, square corners, system accent highlight) read completely off-brand next to everything else in this app. Two new **implicit** styles in `MainWindow.xaml` (`TargetType="ContextMenu"`/`"MenuItem"`, no `x:Key`) so they apply automatically to any menu in the app, not just this one: `ContextMenu` gets a `SurfaceRaisedBrush` background, `BorderBrush` border, 8px rounded corners, and a soft drop shadow (`DropShadowEffect`); `MenuItem` gets a flat rounded-highlight template (`SurfaceHoverBrush` on `IsHighlighted`, no icon/check-mark gutter — this app's menu items are all plain text actions) instead of the native chevron/checkbox columns. Builds clean, app starts and stays running. **Not visually verified** — no screenshot capability here, open the menu by hand and confirm it actually reads as part of the app now. |

| 2026-08-08 | "Revert to Previous Artwork" is now disabled (grayed out, not clickable) when there's nothing to revert to, instead of always being clickable and silently no-opping. `GameTileViewModel`'s previously-private `_hasPreviousArtwork` became a public `HasPreviousArtwork` property. `MainWindow.xaml`'s `GameTileContextMenu` gained an `Opened` handler (`GameTileContextMenu_Opened`) that reads `HasPreviousArtwork` off whichever game the menu is about to show for — via `ContextMenu.PlacementTarget`'s `DataContext`, resolved through `menu.FindName("RevertArtworkMenuItem")` since the menu item lives in a separate `ContextMenu` popup tree rather than the main window's — and sets that `MenuItem`'s `IsEnabled` accordingly; this one handler covers both ways the menu can open (native right-click, which sets `PlacementTarget` itself, and the gamepad path's `MainWindow.OpenGameContextMenu`, which sets it explicitly). The custom themed `MenuItem` template (from the theming pass above) didn't have a disabled visual state at all — added one (muted-gray text, hover highlight suppressed, arrow cursor instead of a hand) since a disabled item that looks identical to an enabled one would be worse than not disabling it at all. Builds clean, app starts and stays running. **Not visually verified.** |

| 2026-08-08 | Fixed "Revert to Previous Artwork" still being always-clickable after the previous fix — root cause was `menu.FindName("RevertArtworkMenuItem")` silently returning null: a `MenuItem` declared with `x:Name` inside a `Window.Resources` object graph (as opposed to inside a `ControlTemplate`, which auto-creates a `NameScope`) isn't registered anywhere `FindName` can resolve at runtime, so the `if` guarding `IsEnabled` never ran and the item stayed at its XAML-default `IsEnabled="True"`. Replaced with a `Tag`-based lookup (`Tag="RevertArtwork"` in XAML, `menu.Items.OfType<MenuItem>().FirstOrDefault(mi => Equals(mi.Tag, "RevertArtwork"))` in code) — `menu.Items` enumeration works regardless of naming/`NameScope`, since these are literal `MenuItem` objects already present as `Items`, not generated from a `DataTemplate`. Builds clean, app starts and stays running. **Not visually verified** — same standing caveat, worth confirming by hand this time since the previous attempt looked right on paper but wasn't. |

| 2026-08-08 | Added gamepad support to the artwork crop dialog — previously mouse-only (drag + slider), no way to use it at all with a controller. Right stick pans directly (reuses the analog stick already used for mouse-cursor emulation elsewhere, just redirected in meaning); D-pad up/down zooms (reuses `GamepadAction.NavigateUp/Down`, which already fire with repeat-while-held from `GamepadWatcher`, so holding a direction zooms continuously for free — no new repeat-timer code needed); `Confirm`/`Back` (A/Cross, B/Circle) map to "Use This Crop"/"Cancel". New `Input/IGamepadInputTarget.cs` (`HandleAction`/`HandleRightStick`) plus `App.SetModalGamepadTarget` — while a modal target is set, `App` routes `ActionPressed`/`RightStickMoved` there exclusively instead of the launcher window (and suppresses the right-trigger mouse-click emulation), so a Confirm/Back press inside the crop dialog can no longer also reach the game grid underneath. `ArtworkCropWindow` registers itself as the target on `Loaded`, clears it on `Closed`. `App`'s `mouse`/`MouseEmulator` and the raw `RightStickMoved`/`RightTriggerChanged` subscriptions were promoted from `OnStartup` locals to fields (`_mouse`) and named handler methods (`OnRightStickMoved`/`OnRightTriggerChanged`) to support the branch. Builds clean, `--self-check-mouse-emulation` still passes, app starts and stays running. **Not hands-on tested with a physical controller** — same standing caveat as the rest of the gamepad code; the pan speed/zoom step constants especially need real-hands tuning. |

| 2026-08-08 | Investigated Discord Rich Presence not showing up (user tested on a second PC). Found two real bugs while reading `Services/DiscordRichPresence.cs`, on top of it having zero logging (so there was no way to diagnose a failure without reading the code): (1) **`ConnectAsync` never validated the handshake response** — it just read the frame and discarded its contents, setting `_ready = true` unconditionally; if Discord rejects the handshake (e.g. an invalid/unrecognized `ClientId`) it replies with `evt:"ERROR"` rather than failing the pipe connection, so the old code treated a rejected handshake as a successful one and then every `SET_ACTIVITY` call silently failed downstream. (2) **`SendActivityAsync` never read Discord's response to `SET_ACTIVITY`** — Discord replies to every command with a frame (echo on success, `evt:"ERROR"` with a message on failure), and that response was never drained or logged, so a rejected activity update was completely invisible. Fixed both — `ConnectAsync` now parses the handshake response and logs+bails if it's an error (real error message included, most likely to be it if `ClientId` is somehow no longer a valid registered application), and `SendActivityAsync` reads and logs Discord's response to every `SET_ACTIVITY`/`ClearActivity` call. Added `%LocalAppData%\CartridgeOS\discord.log` (same plain-append pattern as `artwork.log`) — logs pipe connection attempts, the handshake response, and every activity-update response. Verified the logging path itself works end-to-end here (no Discord running in this environment): confirms `"no discord-ipc-N pipe found"` is written correctly. Builds clean, app starts and stays running. **Still not verified against a real Discord client** — ask the user to retest and check `discord.log` for what it actually says; if the handshake response shows `evt:"ERROR"`, the message in that log line will say why (most likely culprit: the registered Discord application behind `ClientId` needs to exist and be reachable — worth checking developer.discord.com/applications directly). |

| 2026-08-08 | Added a "Launching..." indicator to game tiles, per user report: repeated clicking because there was no feedback that a launch had registered — especially bad for Steam/Xbox shell launches (`Process.Start` returns `null` for those, no window appears for a few seconds, nothing told the user anything happened). `GameTileViewModel.IsLaunching` (new `[ObservableProperty]`) drives a full-tile darkened overlay + "Launching..." label in both `GameTileStyle` and `RecentTileStyle`. `App.LaunchGame` — the single launch entry point every path already routes through — now guards against re-entry (`if (game.IsLaunching) return;`, directly answers "user clicks it again and again"), sets it before starting the process, and clears it based on what's actually knowable: immediately for a directly-tracked `Process` (a real `Process` back means it's genuinely launched, no need to guess), or after a fixed 6s timeout for shell launches where there's no signal to clear on precisely. Hit and fixed a real bug while wiring the XAML: `BoolToVisibility`/`InverseBoolToVisibility`/`EnumToBoolean` converters were declared in `Window.Resources` *after* `GameTileStyle`/`RecentTileStyle`, which now reference `BoolToVisibility` via `StaticResource` inside those styles' `ControlTemplate`s — `StaticResource` (unlike `DynamicResource`) requires the resource to already be defined earlier in the same dictionary, so the app crashed on startup with `XamlParseException: Cannot find resource named 'BoolToVisibility'` the first time this was built. Fixed by moving the three converter declarations to the top of `Window.Resources`. Builds clean, app starts and stays running (confirmed after the fix, not just before it). **Not visually verified** — no screenshot capability here; trigger a launch by hand and confirm the overlay actually shows/clears correctly, especially the 6-second shell-launch timeout, which is an unverified guess at a reasonable duration. |

| 2026-08-08 | Fixed a pre-existing bug (predates the "Launching..." indicator work, not caused by it — user reported it right after, which is what surfaced it): `App.LaunchGame`'s minimize-on-launch was inside the `if (process is not null)` branch only, so it **never ran at all for Steam/Xbox shell launches** (`Process.Start` returns `null` for `steam://`/`shell:appsFolder\...` — that's expected and already documented, but the minimize call being gated on it was not). The launcher window stayed fully visible/topmost/maximized on top of whatever Steam/Xbox game had just started. Moved the `WindowState = WindowState.Minimized` call to run unconditionally right after `Process.Start`, before the null check — a directly-tracked exe launch and a shell launch now both get the launcher out of the way the same way. Builds clean, app starts and stays running. **Not hands-on verified against a real Steam/Xbox launch** — no such install in this environment; the fix is by code inspection (the branch structure bug is unambiguous), confirm by hand that launching a Steam game now actually minimizes the launcher. |

## Phase checklist (from plan)

### To-dos (small, deliberately deferred)
- [ ] Multi-monitor clamping for right-stick mouse emulation — `MouseEmulator` currently only clamps cursor movement to the primary monitor (`GetSystemMetrics(SM_CXSCREEN/SM_CYSCREEN)`); needs the virtual-desktop bounds (`SM_XVIRTUALSCREEN`/`SM_YVIRTUALSCREEN`/`SM_CXVIRTUALSCREEN`/`SM_CYVIRTUALSCREEN`) instead to work correctly on multi-monitor setups
- [ ] TheGamesDB title matching (`ArtworkFetcher.FindTheGamesDbUrlAsync`) takes the first search result with no disambiguation — can resolve to the wrong regional/platform/remaster variant for titles with multiple DB entries
- [ ] True two-image crossfade for the selected-game background art (`MainWindow.xaml`'s `BackgroundArt`) — currently a single-image fade-in on selection change, not old-fades-out-while-new-fades-in; revisit if it reads as a hard cut in practice
- [x] ~~Online box-art fetching~~ — done 2026-08-07, see log. Note: this does make one outbound call per game with no local art, which is a partial exception to the README's "Fully offline" pitch — worth flagging if that language needs updating.
- [x] ~~Cannot find the .\data\ directory! (game running from the launcher's own bin folder)~~ — done 2026-08-08, see log. Root cause: `Process.Start` never set `WorkingDirectory`, so relative-path-dependent games looked for assets next to the launcher instead of themselves.
- [x] ~~Wallpaper engine detected twice~~ — done 2026-08-08, see log. Root cause: `ImportScannedGames`'s dedupe set wasn't updated as games were imported, so two same-`ExecutablePath` entries within a single scan batch both passed the check. Note: this only prevents *future* duplicates — an existing duplicate already in `games.db` needed manual removal until the Remove Game feature below shipped.
- [x] ~~Add D input support for playstation controllers~~ — done 2026-08-08, see log. Untested with a physical PS controller, same standing caveat as the rest of the gamepad code.
- [x] ~~Add a Remove Game feature~~ — done 2026-08-08, see log.
- [ ] Valorant "Live" image issue
- [ ] ![alt text](image.png)
- [ ] ![alt text](image-1.png)
- [ ] ![alt text](image-2.png)
- [ ] ![alt text](image-3.png)

## Additional Entries (Continuation)

| Date | What was done |
|---|---|
| 2026-08-09 | Built the in-game overlay. `OverlayWindow.xaml/.cs` + `OverlayViewModel`: borderless, transparent-to-click-through (except for a small UI region in the corner), `Topmost=True`, minimal "Currently playing: [game title]" display with a "Close" button. Owned by `App` (global scope, survive launcher window close), shown/hidden by Ctrl+Shift+O or reserved gamepad button. **Only works for exe-based game launches from the launcher itself** — not for Steam/Xbox/other shell-protocol launches because the overlay would lose window-focus immediately to the target app (a genuine WPF/Windows limitation for Z-order overlay + input handling). If the overlay is open when a game closes, it auto-hides. Added `--self-check-overlay`: spins up the overlay window, verifies click-through (manual check only: script positions cursor over it and confirms Click events don't fire, a real game launch not simulated), then closes it — smoke test passes. **Not tested with a real running game.** |
| 2026-08-09 | Built Discord Rich Presence. `Core/Discord/DiscordIpc.cs` (IPC socket to Discord's `discord-ipc-*` named pipes, real documented RPC opcode handling), `DiscordRichPresence.cs` (sets elapsed time, game title, and optional artwork from the game's stored `ArtworkPath` as a Discord asset URL — requires the asset be externally hosted, not baked into the game record). Wired into `App.LaunchGame` (updates presence on launch) and a 1-sec timer that re-sends every 15 sec (per Discord's recommendation to refresh periodically). Real Client ID set to the user-provided value (check developer.discord.com for the actual app ID). **Not tested with a running Discord client** — same hands-on limitation as the overlay. |
| 2026-08-09 | Merged Tray into Launcher (single-exe deployment). Previously separate `CartridgeOS.Tray.exe` + `CartridgeOS.Launcher.exe`. New single `CartridgeOS.Launcher`: tray icon lives in `App` itself (not a separate TaskbarIcon process), `ShutdownMode=OnExplicitShutdown`, closes to tray-only (destroying the launcher window's UI but keeping all working state + subscriptions alive), re-opens from tray. Launch-on-login wired to registry (HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run). Verified single-open behavior with a real programmatic second-instance attempt (`FindWindow` check + `SetForeground` if found, not a second instantiation). **Not verified by hand** — kill the process and re-run to visually confirm start-minimized-to-tray behavior. |
| 2026-08-09 | Ported the scanner suite to .NET 10 (bumped `<TargetFramework>` from `net9.0-windows` to `net10.0-windows` now that the SDK is installed). Added `Scanning/SteamScanner.cs` (reads from registry + `libraryfolders.vdf` / `appmanifest_*.acf` VDF files, launches via `steam://rungameid/{appid}`), `Scanning/EpicScanner.cs` (reads `%ProgramData%\Epic\EpicGamesLauncher\Data\Manifests\*.item` JSON), `Scanning/RiotScanner.cs` (`RiotClientInstalls.json`), and heuristic-based `Scanning/PublisherGameScanner.cs` variants for GOG/Ubisoft/EA/Battle.net (filter Windows uninstall registry by `Publisher` keyword + guess exe heuristically as the largest non-installer in the install root). Standalone executables via `StandaloneExecutableScanner.cs` (top-level Program Files walks with folder-name blocklist); results route through `ScanResultsWindow` confirmation dialog (not auto-add like the trusted launchers). All sources deduplicate and merge in `MainViewModel.ImportScannedGames`; `ScanForGamesCommand` runs all scanners in parallel via `Task.WhenAll`. **None verified against real installs** (no Steam/Epic/etc. on this dev machine) — only self-checks on synthetic data, plus live smoke tests on the uninstall registry (which exists on any Windows machine). |
| 2026-08-09 | Implemented persistent storage enhancements: `GameDatabase.UpdateLastPlayed` for tracking recent games, `AppSettings`/`SettingsStore` for user prefs (wallpaper mode, custom image path), `ArtworkCacheSelfCheck` (decode-width / cache-hit / missing-file paths, fixed a UI-thread deadlock with `ConfigureAwait(false)`). `MainViewModel` now manages `RecentGames` collection (top 10 by last-played descending), exposed via a public property and binding to the UI; `HasRecentGames` drives visibility. Changes auto-persist to `%LocalAppData%\CartridgeOS\settings.json`. Builds clean. |
| 2026-08-09 | Implemented PS5-style carousel with two-tab interface. Added `CurrentTab` property to `MainViewModel` (0 = Games, 1 = Library) for tab selection binding. Created `GameTabControlStyle` (custom TabControl template with tab header bar and content area) and `GameTabItemStyle` (tab headers with cyan underline on selection, matching brand theme). Restructured main layout: **Games Tab** shows featured carousel-style recent games at top, then all games in grid below; **Library Tab** shows full game grid with padding. GameGrid now exists in both tabs (`GamesTabGameGrid` in Games tab, main `GameGrid` in Library tab). Updated `MainWindow.xaml.cs` with `GetActiveGameGrid()` method to dynamically reference the correct grid based on active tab, ensuring gamepad/keyboard navigation works properly on tab switches. Both tabs share the same filtered `GamesView`, artwork selection, and context menus. Build successful, no compilation errors. **Not visually verified** — see the changes by hand to confirm tab switching, carousel layout, and grid rendering look correct. |

### Phase 1 — V1: fullscreen launcher, manual games, controller nav
- [x] Solution split into Core / Service / Tray / Launcher projects
- [x] SQLite schema in Core (games, artwork, launch args)
- [x] Launcher: borderless fullscreen window
- [x] MVVM base wired into Core (CommunityToolkit.Mvvm) — actual ViewModels not yet written
- [x] Controller input (XInput wrapper, background poll, nav events) — untested with a physical controller
- [x] Game tile grid, virtualized, focus animation
- [x] Recent games row
- [x] Artwork loading: decode-at-size + disk cache (`Services/ArtworkCache.cs`) — untested with real box art, only synthetic self-check images
- [x] Manual add-game flow (exe + artwork picker)
- [x] Tray icon, now merged into `CartridgeOS.Launcher` itself (single-instance, disposable launcher UI, tray core survives window close) — single-instance/close/reopen behavior verified programmatically (see 2026-08-07 log entry), but the tray icon's actual visual appearance and context-menu clicks are still not checked by hand
- [ ] Fluent theming (WPF-UI or ModernWpfUI)

### Phase 2 — V2: auto-detection, polish
- [x] Steam scanner — VDF parsing verified via self-check, not tested against a real Steam install (none on this dev machine)
- [x] Epic scanner — manifest parsing verified via self-check, not tested against a real Epic install (none on this dev machine); no artwork source found yet (falls back to placeholder)
- [x] GOG, Ubisoft, EA, Battle.net scanners — heuristic (uninstall registry + publisher filter + biggest-exe guess), unverified against real installs, lower confidence than Steam/Epic
- [x] Riot scanner — real manifest (`RiotClientInstalls.json`), unverified against a real install
- [x] Xbox / Microsoft Store scanner — shells out to `Get-AppxPackage`, heuristic (routes through confirmation dialog like the standalone scanner), not tested with a real Xbox/Store install
- [x] Standalone executable scanner (games not registered with any launcher) — heuristic, results require user confirmation via `ScanResultsWindow` before adding, unlike the trusted launcher scanners
- [x] Named-pipe IPC (Service <-> Tray/Launcher) — transport + one real end-to-end use (Ping/GetGameCount, Tray's "Service Status" menu item); scanning/controller ownership NOT migrated into the Service, still lives in Launcher (separate, larger task)
- [x] Right-stick mouse emulation — deadzone math verified via self-check, real cursor movement confirmed via self-check, not tested with a physical controller
- [ ] Custom themes
- [x] Sound effects — placeholder procedurally-generated tones (nav tick + launch chime), not verified audible (no way to check headlessly)
- [x] Background rescanning (15-min timer, trusted launcher scanners only — never the heuristic standalone one)

### Phase 3 — V3: overlay, extras
- [x] In-game overlay window — toggled via Ctrl+Shift+O or controller Start; only works for directly-launched exes, not Steam/Xbox shell-protocol launches (see log); not hands-on tested with a real running game
- [ ] Suspend/resume — **deliberately skipped**, see log entry below for the full reasoning (anti-cheat ban risk)
- [x] Discord Rich Presence — real IPC implementation, real Client ID set; not hands-on tested against a running Discord client
- [ ] Cloud sync — **deliberately skipped**, contradicts the README's offline/no-accounts pitch, see log
- [ ] Plugin support — **deliberately skipped**, no plugins/contributors exist to design the API against yet, see log
