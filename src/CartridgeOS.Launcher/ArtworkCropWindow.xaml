<Window x:Class="CartridgeOS.Launcher.ArtworkCropWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Adjust Artwork"
        Width="360" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}">
    <StackPanel Margin="20">
        <TextBlock Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,12"
                   Text="This image doesn't match the tile's shape — this is what will show on the tile. Drag/slider or right stick/D-pad to reposition and zoom, A to confirm, B to cancel." />

        <Border Width="300" Height="450" HorizontalAlignment="Left" ClipToBounds="True" CornerRadius="8"
                Background="{StaticResource SurfaceRaisedBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                Cursor="SizeAll"
                MouseLeftButtonDown="Viewport_MouseLeftButtonDown"
                MouseLeftButtonUp="Viewport_MouseLeftButtonUp"
                MouseMove="Viewport_MouseMove">
            <Canvas>
                <Image x:Name="PreviewImage" Stretch="None" RenderTransformOrigin="0,0">
                    <Image.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="ImageScale" />
                            <TranslateTransform x:Name="ImageTranslate" />
                        </TransformGroup>
                    </Image.RenderTransform>
                </Image>
            </Canvas>
        </Border>

        <Slider x:Name="ZoomSlider" Margin="0,12,0,0" ValueChanged="ZoomSlider_ValueChanged" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Click="Cancel_Click" Padding="16,8" Margin="0,0,10,0"
                    Background="{StaticResource SurfaceRaisedBrush}" Foreground="{StaticResource TextPrimaryBrush}" BorderThickness="0" />
            <Button Content="Use This Crop" Click="UseCrop_Click" Style="{StaticResource PrimaryButtonStyle}" />
        </StackPanel>
    </StackPanel>
</Window>
