namespace CartridgeOS.Launcher.ViewModels;

/// <summary>The launcher's top-level screens, selected via the Home/Recently Played/Library nav bar.</summary>
public enum AppScreen
{
    Home,
    RecentlyPlayed,
    Library,
}
